---
title: Directories
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Directories" playHash="6saPJU8IfPL" next={{title: "Temporary Files and Directories", href: "/go/temporary-files-and-directories"}}>

<GoExampleRow docs={"Go has several useful functions for working with *directories* in the file system."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"directories.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"io/fs\"\n    \"os\"\n    \"path/filepath\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func check(e error) {\n    if e != nil {\n        panic(e)\n    }\n}"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"Create a new sub-directory in the current working directory."} language="go" code={"    err := os.Mkdir(\"subdir\", 0755)\n    check(err)"} />

<GoExampleRow docs={"When creating temporary directories, it's good practice to `defer` their removal. `os.RemoveAll` will delete a whole directory tree (similarly to `rm -rf`)."} language="go" code={"    defer os.RemoveAll(\"subdir\")"} />

<GoExampleRow docs={"Helper function to create a new empty file."} language="go" code={"    createEmptyFile := func(name string) {\n        d := []byte(\"\")\n        check(os.WriteFile(name, d, 0644))\n    }"} />

<GoExampleRow docs={undefined} language="go" code={"    createEmptyFile(\"subdir/file1\")"} />

<GoExampleRow docs={"We can create a hierarchy of directories, including parents with `MkdirAll`. This is similar to the command-line `mkdir -p`."} language="go" code={"    err = os.MkdirAll(\"subdir/parent/child\", 0755)\n    check(err)"} />

<GoExampleRow docs={undefined} language="go" code={"    createEmptyFile(\"subdir/parent/file2\")\n    createEmptyFile(\"subdir/parent/file3\")\n    createEmptyFile(\"subdir/parent/child/file4\")"} />

<GoExampleRow docs={"`ReadDir` lists directory contents, returning a slice of `os.DirEntry` objects."} language="go" code={"    c, err := os.ReadDir(\"subdir/parent\")\n    check(err)"} />

<GoExampleRow docs={undefined} language="go" code={"    fmt.Println(\"Listing subdir/parent\")\n    for _, entry := range c {\n        fmt.Println(\" \", entry.Name(), entry.IsDir())\n    }"} />

<GoExampleRow docs={"`Chdir` lets us change the current working directory, similarly to `cd`."} language="go" code={"    err = os.Chdir(\"subdir/parent/child\")\n    check(err)"} />

<GoExampleRow docs={"Now we'll see the contents of `subdir/parent/child` when listing the *current* directory."} language="go" code={"    c, err = os.ReadDir(\".\")\n    check(err)"} />

<GoExampleRow docs={undefined} language="go" code={"    fmt.Println(\"Listing subdir/parent/child\")\n    for _, entry := range c {\n        fmt.Println(\" \", entry.Name(), entry.IsDir())\n    }"} />

<GoExampleRow docs={"`cd` back to where we started."} language="go" code={"    err = os.Chdir(\"../../..\")\n    check(err)"} />

<GoExampleRow docs={"We can also visit a directory *recursively*, including all its sub-directories. `WalkDir` accepts a callback function to handle every file or directory visited."} language="go" code={"    fmt.Println(\"Visiting subdir\")\n    err = filepath.WalkDir(\"subdir\", visit)\n    check(err)\n}"} />

<GoExampleRow docs={"`visit` is called for every file or directory found recursively by `filepath.WalkDir`."} language="go" code={"func visit(path string, d fs.DirEntry, err error) error {\n    if err != nil {\n        return err\n    }\n    fmt.Println(\" \", path, d.IsDir())\n    return nil\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go run directories.go\nListing subdir/parent\n  child true\n  file2 false\n  file3 false\nListing subdir/parent/child\n  file4 false\nVisiting subdir\n  subdir true\n  subdir/file1 false\n  subdir/parent true\n  subdir/parent/child true\n  subdir/parent/child/file4 false\n  subdir/parent/file2 false\n  subdir/parent/file3 false"} />

</GoExample>
