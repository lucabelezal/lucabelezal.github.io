---
title: Hello World
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Hello World" playHash="NeviD0awXjt" next={{title: "Values", href: "/go/values"}}>

<GoExampleRow docs={<>Nosso primeiro programa imprime o clássico “hello world”. Aqui está o código completo.</>} code={`package main

import "fmt"

func main() {`} />

<GoExampleRow docs={<></>} code={`    fmt.Println("hello world")
}`} />

<GoExampleRow docs={<>Para rodar o programa, salve o código em `hello-world.go` e use `go run`.</>} code={`$ go run hello-world.go
hello world`} />

<GoExampleRow docs={<>Às vezes queremos compilar em um binário.</>} code={`$ go build hello-world.go
$ ls
hello-world    hello-world.go`} />

<GoExampleRow docs={<>E executamos o binário diretamente.</>} code={`$ ./hello-world
hello world`} />

<GoExampleRow docs={<>Agora que rodamos e compilamos um programa básico, vamos aprender mais sobre a linguagem.</>} empty code={``} />

</GoExample>
