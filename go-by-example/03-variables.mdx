---
title: Variáveis
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Variáveis"}
  code={`package main

import "fmt"

func main() {
    var a = "initial"
    fmt.Println(a)
    var b, c int = 1, 2
    fmt.Println(b, c)
    var d = true
    fmt.Println(d)
    var e int
    fmt.Println(e)
    f := "apple"
    fmt.Println(f)
}`}
  sections={[
    {text: "`var` declara uma ou mais variáveis."},
    {text: "Você pode declarar múltiplas variáveis de uma vez."},
    {text: "Go infere o tipo de variáveis inicializadas."},
    {text: "Variáveis declaradas sem inicialização recebem o _zero value_. Por exemplo, o zero value de um `int` é `0`."},
    {text: "A sintaxe `:=` é um atalho para declarar e inicializar uma variável, por exemplo `var f string = \"apple\"` neste caso. Essa sintaxe só está disponível dentro de funções."},
    {text: "Saída:\n$ go run variables.go\ninitial\n1 2\ntrue\n0\napple"}
  ]}
/>

<a href="https://go.dev/play/p/N5rWndIliJW" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
