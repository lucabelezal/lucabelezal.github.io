---
title: Tickers
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Tickers" playHash="gs6zoJP-Pl9" next={{title: "Worker Pools", href: "/go/worker-pools"}}>

<GoExampleRow docs={"[Timers](timers) are for when you want to do something once in the future - _tickers_ are for when you want to do something repeatedly at regular intervals. Here's an example of a ticker that ticks periodically until we stop it."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"tickers.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"time\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"Tickers use a similar mechanism to timers: a channel that is sent values. Here we'll use the `select` builtin on the channel to await the values as they arrive every 500ms."} language="go" code={"    ticker := time.NewTicker(500 * time.Millisecond)\n    done := make(chan bool)"} />

<GoExampleRow docs={undefined} language="go" code={"    go func() {\n        for {\n            select {\n            case <-done:\n                return\n            case t := <-ticker.C:\n                fmt.Println(\"Tick at\", t)\n            }\n        }\n    }()"} />

<GoExampleRow docs={"Tickers can be stopped like timers. Once a ticker is stopped it won't receive any more values on its channel. We'll stop ours after 1600ms."} language="go" code={"    time.Sleep(1600 * time.Millisecond)\n    ticker.Stop()\n    done <- true\n    fmt.Println(\"Ticker stopped\")\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"# When we run this program the ticker should tick 3 times\n# before we stop it.\n$ go run tickers.go\nTick at 2012-09-23 11:29:56.487625 -0700 PDT\nTick at 2012-09-23 11:29:56.988063 -0700 PDT\nTick at 2012-09-23 11:29:57.488076 -0700 PDT\nTicker stopped"} />

</GoExample>
