---
title: Command-Line Subcommands
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Command-Line Subcommands" playHash="DkvdHKK-XCv" next={{title: "Environment Variables", href: "/go/environment-variables"}}>

<GoExampleRow docs={"Some command-line tools, like the `go` tool or `git` have many *subcommands*, each with its own set of flags. For example, `go build` and `go get` are two different subcommands of the `go` tool. The `flag` package lets us easily define simple subcommands that have their own flags."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"command-line-subcommands.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"flag\"\n    \"fmt\"\n    \"os\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"We declare a subcommand using the `NewFlagSet` function, and proceed to define new flags specific for this subcommand."} language="go" code={"    fooCmd := flag.NewFlagSet(\"foo\", flag.ExitOnError)\n    fooEnable := fooCmd.Bool(\"enable\", false, \"enable\")\n    fooName := fooCmd.String(\"name\", \"\", \"name\")"} />

<GoExampleRow docs={"For a different subcommand we can define different supported flags."} language="go" code={"    barCmd := flag.NewFlagSet(\"bar\", flag.ExitOnError)\n    barLevel := barCmd.Int(\"level\", 0, \"level\")"} />

<GoExampleRow docs={"The subcommand is expected as the first argument to the program."} language="go" code={"    if len(os.Args) < 2 {\n        fmt.Println(\"expected 'foo' or 'bar' subcommands\")\n        os.Exit(1)\n    }"} />

<GoExampleRow docs={"Check which subcommand is invoked."} language="go" code={"    switch os.Args[1] {"} />

<GoExampleRow docs={"For every subcommand, we parse its own flags and have access to trailing positional arguments."} language="go" code={"    case \"foo\":\n        fooCmd.Parse(os.Args[2:])\n        fmt.Println(\"subcommand 'foo'\")\n        fmt.Println(\"  enable:\", *fooEnable)\n        fmt.Println(\"  name:\", *fooName)\n        fmt.Println(\"  tail:\", fooCmd.Args())\n    case \"bar\":\n        barCmd.Parse(os.Args[2:])\n        fmt.Println(\"subcommand 'bar'\")\n        fmt.Println(\"  level:\", *barLevel)\n        fmt.Println(\"  tail:\", barCmd.Args())\n    default:\n        fmt.Println(\"expected 'foo' or 'bar' subcommands\")\n        os.Exit(1)\n    }\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go build command-line-subcommands.go \n\n# First invoke the foo subcommand.\n$ ./command-line-subcommands foo -enable -name=joe a1 a2\nsubcommand 'foo'\n  enable: true\n  name: joe\n  tail: [a1 a2]\n\n# Now try bar.\n$ ./command-line-subcommands bar -level 8 a1\nsubcommand 'bar'\n  level: 8\n  tail: [a1]\n\n# But bar won't accept foo's flags.\n$ ./command-line-subcommands bar -enable a1\nflag provided but not defined: -enable\nUsage of bar:\n  -level int\n    \tlevel\n\n# Next we'll look at environment variables, another common\n# way to parameterize programs."} />

</GoExample>
