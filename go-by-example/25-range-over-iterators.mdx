---
title: Range sobre Iteradores
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Range sobre Iteradores"}
  code={`package main

import (
    "fmt"
    "iter"
    "slices"
    "strings"
)

type List[T any] struct {
    head, tail *element[T]
}
type element[T any] struct {
    next *element[T]
    val  T
}
func (lst *List[T]) Push(v T) {
    if lst.tail == nil {
        lst.head = &element[T]{val: v}
        lst.tail = lst.head
    } else {
        lst.tail.next = &element[T]{val: v}
        lst.tail = lst.tail.next
    }
}
func (lst *List[T]) All() iter.Seq[T] {
    return func(yield func(T) bool) {
        for e := lst.head; e != nil; e = e.next {
            if !yield(e.val) {
                return
            }
        }
    }
}
func genFib() iter.Seq[int] {
    return func(yield func(int) bool) {
        a, b := 0, 1
        for {
            if !yield(a) {
                return
            }
            a, b = b, a+b
        }
    }
}
func main() {
    lst := List[int]{}
    lst.Push(10)
    lst.Push(13)
    lst.Push(23)
    for e := range lst.All() {
        fmt.Println(e)
    }
    all := slices.Collect(lst.All())
    fmt.Println("all:", all)
    for part := range strings.SplitSeq("go-by-example", "-") {
        fmt.Printf("part: %s\
", part)
    }
    for n := range genFib() {`}
  sections={[
    {text: "Vejamos o tipo `List` do [exemplo anterior](generics). Lá tínhamos `AllElements` que retornava um slice. Com iteradores de Go, podemos fazer melhor — como abaixo."},
    {text: "`All` retorna um _iterador_, que em Go é uma função com [assinatura especial](https://pkg.go.dev/iter#Seq)."},
    {text: "A função iteradora recebe outra função como parâmetro, chamada `yield` por convenção. Ela chama `yield` para cada elemento e observa o retorno para possível término antecipado."},
    {text: "Iteração não exige estrutura subjacente, nem precisa ser finita! Aqui uma função que retorna iterador sobre Fibonacci: continua enquanto `yield` retornar `true`."},
    {text: "Como `List.All` retorna um iterador, podemos usá-lo em um `range` normal."},
    {text: "Pacotes como [slices](https://pkg.go.dev/slices) têm funções úteis para iteradores. Por exemplo, `Collect` coleta todos os valores em um slice."},
    {text: "Pacotes da stdlib também expõem helpers de iterador. Por exemplo, `strings.SplitSeq` itera sobre partes sem construir um slice intermediário."},
    {text: "Saída:\n$ go run range-over-iterators.go\n10\n13\n23\nall: [10 13 23]\npart: go\npart: by\npart: example\n0\n1\n1\n2\n3\n5\n8"}
  ]}
/>

<a href="https://go.dev/play/p/lHX3uDqdfUB" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
