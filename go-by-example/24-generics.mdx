---
title: Generics
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Generics"}
  code={`package main

import "fmt"

func SlicesIndex[S ~[]E, E comparable](s S, v E) int {
    for i := range s {
        if v == s[i] {
            return i
        }
    }
    return -1
}

type List[T any] struct {
    head, tail *element[T]
}
type element[T any] struct {
    next *element[T]
    val  T
}

func (lst *List[T]) Push(v T) {
    if lst.tail == nil {
        lst.head = &element[T]{val: v}
        lst.tail = lst.head
    } else {
        lst.tail.next = &element[T]{val: v}
        lst.tail = lst.tail.next
    }
}
func (lst *List[T]) AllElements() []T {
    var elems []T
    for e := lst.head; e != nil; e = e.next {
        elems = append(elems, e.val)
    }
    return elems
}
func main() {
    var s = []string{"foo", "bar", "zoo"}
    fmt.Println("index of zoo:", SlicesIndex(s, "zoo"))
    _ = SlicesIndex[[]string, string](s, "zoo")
    lst := List[int]{}
    lst.Push(10)
    lst.Push(13)
    lst.Push(23)
    fmt.Println("list:", lst.AllElements())
}`}
  sections={[
    {text: "Como exemplo de função genérica, `SlicesIndex` recebe um slice de qualquer tipo `comparable` e um elemento, retornando o índice da primeira ocorrência ou -1. A restrição `comparable` permite comparar com `==` e `!=`. Veja [este post](https://go.dev/blog/deconstructing-type-parameters). Note que ela existe na stdlib como [slices.Index](https://pkg.go.dev/slices#Index)."},
    {text: "Como exemplo de tipo genérico, `List` é uma lista encadeada com valores de qualquer tipo."},
    {text: "Podemos definir métodos em tipos genéricos como em tipos normais, mas mantendo os parâmetros de tipo. O tipo é `List[T]`, não `List`."},
    {text: "`AllElements` retorna todos os elementos como slice. No próximo exemplo veremos uma forma mais idiomática de iterar sobre tipos customizados."},
    {text: "Ao chamar funções genéricas, podemos confiar em _inferência de tipo_. Não precisamos especificar `S` e `E` em `SlicesIndex` — o compilador infere."},
    {text: "... embora também pudéssemos especificar explicitamente."},
    {text: "Saída:\n$ go run generics.go\nindex of zoo: 2\nlist: [10 13 23]"}
  ]}
/>

<a href="https://go.dev/play/p/7v7vElzhAeO" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
