---
title: Epoch
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Epoch"}
  code={`package main

import (
    "fmt"
    "time"
)

func main() {
    now := time.Now()
    fmt.Println(now)
    fmt.Println(now.Unix())
    fmt.Println(now.UnixMilli())
    fmt.Println(now.UnixNano())
    fmt.Println(time.Unix(now.Unix(), 0))
    fmt.Println(time.Unix(0, now.UnixNano()))
}`}
  sections={[
    {text: "Use `time.Now` com `Unix`, `UnixMilli` ou `UnixNano` para tempo desde o epoch em segundos, ms ou ns."},
    {text: "Também pode converter segundos ou nanos desde o epoch para `time`."},
    {text: "Saída:\n$ go run epoch.go \n2012-10-31 16:13:58.292387 +0000 UTC\n1351700038\n1351700038292\n1351700038292387000\n2012-10-31 16:13:58 +0000 UTC\n2012-10-31 16:13:58.292387 +0000 UTC\n\n# Next we'll look at another time-related task: time\n# parsing and formatting."}
  ]}
/>

<a href="https://go.dev/play/p/lRmD1EWHHPz" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
