---
title: JSON
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"JSON"}
  code={`package main

import (
    "bytes"
    "encoding/json/v2"
    "fmt"
    "strings"
)

type response1 struct {
    Page   int
    Fruits []string
}
type response2 struct {
    Page   int      \`json:"page"\`
    Fruits []string \`json:"fruits"\`
}

func main() {
    bolB, _ := json.Marshal(true)
    fmt.Println(string(bolB))
    intB, _ := json.Marshal(1)
    fmt.Println(string(intB))
    fltB, _ := json.Marshal(2.34)
    fmt.Println(string(fltB))
    strB, _ := json.Marshal("gopher")
    fmt.Println(string(strB))
    slcD := []string{"apple", "peach", "pear"}
    slcB, _ := json.Marshal(slcD)
    fmt.Println(string(slcB))
    mapD := map[string]int{"apple": 5, "lettuce": 7}
    mapB, _ := json.Marshal(mapD)
    fmt.Println(string(mapB))
    res1D := &response1{
        Page:   1,
        Fruits: []string{"apple", "peach", "pear"}}
    res1B, _ := json.Marshal(res1D)
    fmt.Println(string(res1B))
    res2D := &response2{
        Page:   1,
        Fruits: []string{"apple", "peach", "pear"}}
    res2B, _ := json.Marshal(res2D)
    fmt.Println(string(res2B))
    byt := []byte(\`{"num":6.13,"strs":["a","b"]}\`)
    var dat map[string]any
    if err := json.Unmarshal(byt, &dat); err != nil {
        panic(err)
    }
    fmt.Println(dat)
    num := dat["num"].(float64)
    fmt.Println(num)
    strs := dat["strs"].([]any)
    str1 := strs[0].(string)
    fmt.Println(str1)
    str := \`{"page": 1, "fruits": ["apple", "peach"]}\`
    res := response2{}
    _ = json.Unmarshal([]byte(str), &res)
    fmt.Println(res)
    fmt.Println(res.Fruits[0])
    d := map[string]int{"apple": 5, "lettuce": 7}
    var buf bytes.Buffer
    _ = json.MarshalWrite(&buf, d)
    fmt.Println(buf.String())
    res1 := response2{}
    _ = json.UnmarshalRead(strings.NewReader(str), &res1)
    fmt.Println(res1)
}`}
  sections={[
    {text: "Usaremos estes dois structs para demonstrar encoding e decoding de tipos customizados."},
    {text: "Apenas campos exportados são codificados/decodificados em JSON. Precisam começar com maiúscula."},
    {text: "Primeiro, encoding de tipos básicos para JSON. Aqui exemplos de valores atômicos."},
    {text: "E aqui para slices e maps, que viram arrays e objetos JSON."},
    {text: "O pacote JSON pode codificar tipos customizados automaticamente. Só inclui campos exportados e usa os nomes como chaves."},
    {text: "Pode usar tags nos campos para customizar chaves JSON. Veja `response2` acima."},
    {text: "Agora, decoding de JSON para valores Go. Exemplo com estrutura genérica."},
    {text: "Precisamos de variável para o pacote JSON depositar os dados. `map[string]any` guarda mapa de strings para tipos arbitrários."},
    {text: "Aqui o decoding real e checagem de erro. Por brevidade ignoramos erros; em código real sempre verifique."},
    {text: "Para usar valores do map decodificado, converta para o tipo apropriado. Aqui convertemos `num` para `float64`."},
    {text: "Acessar dados aninhados exige série de conversões."},
    {text: "Também podemos decodificar para tipos customizados. Vantagem: mais type-safety e sem type assertions."},
    {text: "Acima usamos bytes e strings como intermediários. Também podemos fazer stream direto para `io.Writer`s como `os.Stdout` ou bodies HTTP."},
    {text: "Leitura em stream de `io.Reader`s como `os.Stdin` ou bodies HTTP é feita com `json.UnmarshalRead`."},
    {text: "Saída:\n$ go run json.go\ntrue\n1\n2.34\n\"gopher\"\n[\"apple\",\"peach\",\"pear\"]\n{\"apple\":5,\"lettuce\":7}\n{\"Page\":1,\"Fruits\":[\"apple\",\"peach\",\"pear\"]}\n{\"page\":1,\"fruits\":[\"apple\",\"peach\",\"pear\"]}\nmap[num:6.13 strs:[a b]]\n6.13\na\n{1 [apple peach]}\napple\n{\"apple\":5,\"lettuce\":7}\n{1 [apple peach]}\n\n\n# We've covered the basic of JSON in Go here, but check\n# out the [JSON and Go](https://go.dev/blog/json)\n# blog post and the [JSON package docs](https://pkg.go.dev/encoding/json/v2)\n# for more."}
  ]}
/>

<a href="https://go.dev/play/p/09HfDq_PP3G" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [XML](/go/xml).
