---
title: Exec
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Exec"}
  code={`package main

import (
    "os"
    "os/exec"
    "syscall"
)

func main() {
    binary, lookErr := exec.LookPath("ls")
    if lookErr != nil {
        panic(lookErr)
    }
    args := []string{"ls", "-a", "-l", "-h"}
    env := os.Environ()
    execErr := syscall.Exec(binary, args, env)
    if execErr != nil {
        panic(execErr)
    }
}`}
  sections={[
    {text: "Neste exemplo faremos exec de `ls`. Go exige path absoluto, então usamos `exec.LookPath` (provavelmente `/bin/ls`)."},
    {text: "`Exec` exige args em slice (não uma string única). Passamos alguns args comuns. Note que o primeiro deve ser o nome do programa."},
    {text: "`Exec` também precisa de [variáveis de ambiente](environment-variables). Aqui fornecemos o ambiente atual."},
    {text: "Aqui a chamada `syscall.Exec`. Se bem-sucedida, a execução do processo termina e é substituída por `/bin/ls -a -l -h`. Se houver erro, recebemos retorno."},
    {text: "Saída:\n# When we run our program it is replaced by `ls`.\n$ go run execing-processes.go\ntotal 16\ndrwxr-xr-x  4 mark 136B Oct 3 16:29 .\ndrwxr-xr-x 91 mark 3.0K Oct 3 12:50 ..\n-rw-r--r--  1 mark 1.3K Oct 3 16:28 execing-processes.go\n\n# Note that Go does not offer a classic Unix `fork`\n# function. Usually this isn't an issue though, since\n# starting goroutines, spawning processes, and exec'ing\n# processes covers most use cases for `fork`."}
  ]}
/>

<a href="https://go.dev/play/p/s9qg7olf1dM" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Signals](/go/signals).
