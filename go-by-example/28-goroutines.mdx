---
title: Goroutines
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Goroutines"}
  code={`package main

import (
    "fmt"
    "time"
)

func f(from string) {
    for i := range 3 {
        fmt.Println(from, ":", i)
    }
}
func main() {
    f("direct")
    go f("goroutine")
    go func(msg string) {
        fmt.Println(msg)
    }("going")
    time.Sleep(time.Second)
    fmt.Println("done")
}`}
  sections={[
    {text: "Suponha a chamada `f(s)`. Assim a chamamos de forma síncrona."},
    {text: "Para invocar em goroutine, use `go f(s)`. A nova goroutine executa concorrentemente com a chamadora."},
    {text: "Também pode iniciar uma goroutine para uma função anônima."},
    {text: "As duas chamadas rodam assincronamente em goroutines separadas. Espere terminarem (para algo robusto, use [WaitGroup](waitgroups))."},
    {text: "Saída:\n# When we run this program, we see the output of the\n# blocking call first, then the output of the two\n# goroutines. The goroutines' output may be interleaved,\n# because goroutines are being run concurrently by the\n# Go runtime.\n$ go run goroutines.go\ndirect : 0\ndirect : 1\ndirect : 2\ngoroutine : 0\ngoing\ngoroutine : 1\ngoroutine : 2\ndone\n\n# Next we'll look at a complement to goroutines in\n# concurrent Go programs: channels."}
  ]}
/>

<a href="https://go.dev/play/p/0fx_WokYVFO" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
