---
title: Criando Processos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Criando Processos"}
  code={`package main

import (
    "errors"
    "fmt"
    "io"
    "os/exec"
)

func main() {
    dateCmd := exec.Command("date")
    _, err = exec.Command("date", "-x").Output()
    if err != nil {
        if e, ok := errors.AsType[*exec.Error](err); ok {
            fmt.Println("failed executing:", e)
        } else if e, ok := errors.AsType[*exec.ExitError](err); ok {
            exitCode := e.ExitCode()
            fmt.Println("command exit rc =", exitCode)
        } else {
            panic(err)
        }
    }
    grepCmd := exec.Command("grep", "hello")
    grepIn, _ := grepCmd.StdinPipe()
    grepOut, _ := grepCmd.StdoutPipe()
    grepCmd.Start()
    grepIn.Write([]byte("hello grep\
goodbye grep"))
    grepIn.Close()
    grepBytes, _ := io.ReadAll(grepOut)
    grepCmd.Wait()`}
  sections={[
    {text: "Começamos com comando simples sem args que imprime em stdout. `exec.Command` cria objeto para o processo externo."},
    {text: "`Output` e outros métodos retornam `*exec.Error` se houver problema ao executar (path errado) e `*exec.ExitError` se rodou mas saiu com código não-zero."},
    {text: "Em seguida, caso mais elaborado onde enviamos dados para `stdin` e coletamos de `stdout`."},
    {text: "Aqui pegamos pipes de input/output, iniciamos o processo, escrevemos input, lemos output e esperamos terminar."}
  ]}
/>

<a href="https://go.dev/play/p/IxGAEXe0I7U" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Exec'ing Processes](/go/execing-processes).
