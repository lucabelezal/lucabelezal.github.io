---
title: Channel Synchronization
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Channel Synchronization" playHash="Nw-1DzIGk5f" next={{title: "Channel Directions", href: "/go/channel-directions"}}>

<GoExampleRow docs={"We can use channels to synchronize execution across goroutines. Here's an example of using a blocking receive to wait for a goroutine to finish. When waiting for multiple goroutines to finish, you may prefer to use a [WaitGroup](waitgroups)."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"channel-synchronization.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"time\"\n)"} />

<GoExampleRow docs={"This is the function we'll run in a goroutine. The `done` channel will be used to notify another goroutine that this function's work is done."} language="go" code={"func worker(done chan bool) {\n    fmt.Print(\"working...\")\n    time.Sleep(time.Second)\n    fmt.Println(\"done\")"} />

<GoExampleRow docs={"Send a value to notify that we're done."} language="go" code={"    done <- true\n}"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"Start a worker goroutine, giving it the channel to notify on."} language="go" code={"    done := make(chan bool, 1)\n    go worker(done)"} />

<GoExampleRow docs={"Block until we receive a notification from the worker on the channel."} language="go" code={"    <-done\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go run channel-synchronization.go      \nworking...done                  \n\n# If you removed the `<- done` line from this program,\n# the program could exit before the `worker` finished\n# its work, or in some cases even before it started."} />

</GoExample>
