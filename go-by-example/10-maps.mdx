---
title: Maps
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Maps" playHash="5jpkxJ2T0Lv" next={{title: "Functions", href: "/go/functions"}}>

<GoExampleRow docs={"_Maps_ are Go's built-in [associative data type](https://en.wikipedia.org/wiki/Associative_array) (sometimes called _hashes_ or _dicts_ in other languages)."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"maps.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"maps\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"To create an empty map, use the builtin `make`: `make(map[key-type]val-type)`."} language="go" code={"    m := make(map[string]int)"} />

<GoExampleRow docs={"Set key/value pairs using typical `name[key] = val` syntax."} language="go" code={"    m[\"k1\"] = 7\n    m[\"k2\"] = 13"} />

<GoExampleRow docs={"Printing a map with e.g. `fmt.Println` will show all of its key/value pairs."} language="go" code={"    fmt.Println(\"map:\", m)"} />

<GoExampleRow docs={"Get a value for a key with `name[key]`."} language="go" code={"    v1 := m[\"k1\"]\n    fmt.Println(\"v1:\", v1)"} />

<GoExampleRow docs={"If the key doesn't exist, the [zero value](https://go.dev/ref/spec#The_zero_value) of the value type is returned."} language="go" code={"    v3 := m[\"k3\"]\n    fmt.Println(\"v3:\", v3)"} />

<GoExampleRow docs={"The builtin `len` returns the number of key/value pairs when called on a map."} language="go" code={"    fmt.Println(\"len:\", len(m))"} />

<GoExampleRow docs={"The builtin `delete` removes key/value pairs from a map."} language="go" code={"    delete(m, \"k2\")\n    fmt.Println(\"map:\", m)"} />

<GoExampleRow docs={"To remove *all* key/value pairs from a map, use the `clear` builtin."} language="go" code={"    clear(m)\n    fmt.Println(\"map:\", m)"} />

<GoExampleRow docs={"The optional second return value when getting a value from a map indicates if the key was present in the map. This can be used to disambiguate between missing keys and keys with zero values like `0` or `\"\"`. Here we didn't need the value itself, so we ignored it with the _blank identifier_ `_`."} language="go" code={"    _, prs := m[\"k2\"]\n    fmt.Println(\"prs:\", prs)"} />

<GoExampleRow docs={"You can also declare and initialize a new map in the same line with this syntax."} language="go" code={"    n := map[string]int{\"foo\": 1, \"bar\": 2}\n    fmt.Println(\"map:\", n)"} />

<GoExampleRow docs={"The `maps` package contains a number of useful utility functions for maps."} language="go" code={"    n2 := map[string]int{\"foo\": 1, \"bar\": 2}\n    if maps.Equal(n, n2) {\n        fmt.Println(\"n == n2\")\n    }\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"# Note that maps appear in the form `map[k:v k:v]` when\n# printed with `fmt.Println`.\n$ go run maps.go \nmap: map[k1:7 k2:13]\nv1: 7\nv3: 0\nlen: 2\nmap: map[k1:7]\nmap: map[]\nprs: false\nmap: map[bar:2 foo:1]\nn == n2"} />

</GoExample>
