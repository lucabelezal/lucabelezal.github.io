---
title: Maps
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Maps"}
  code={`package main

import (
    "fmt"
    "maps"
)

func main() {
    m := make(map[string]int)
    m["k1"] = 7
    m["k2"] = 13
    fmt.Println("map:", m)
    v1 := m["k1"]
    fmt.Println("v1:", v1)
    v3 := m["k3"]
    fmt.Println("v3:", v3)
    fmt.Println("len:", len(m))
    delete(m, "k2")
    fmt.Println("map:", m)
    clear(m)
    fmt.Println("map:", m)
    _, prs := m["k2"]
    fmt.Println("prs:", prs)
    n := map[string]int{"foo": 1, "bar": 2}
    fmt.Println("map:", n)
    n2 := map[string]int{"foo": 1, "bar": 2}
    if maps.Equal(n, n2) {
        fmt.Println("n == n2")
    }
}`}
  sections={[
    {text: "Defina pares chave/valor com a sintaxe `name[key] = val`."},
    {text: "Imprimir um map com `fmt.Println` mostra todos os pares chave/valor."},
    {text: "Obtenha o valor de uma chave com `name[key]`."},
    {text: "Se a chave não existe, retorna o [zero value](https://go.dev/ref/spec#The_zero_value) do tipo do valor."},
    {text: "O builtin `len` retorna o número de pares chave/valor quando chamado em um map."},
    {text: "O builtin `delete` remove pares chave/valor de um map."},
    {text: "Para remover *todos* os pares chave/valor de um map, use o builtin `clear`."},
    {text: "O segundo retorno opcional ao buscar um valor no map indica se a chave existia. Isso permite distinguir entre chaves ausentes e chaves com zero values como `0` ou `\"\"`. Aqui não precisamos do valor, então ignoramos com o _blank identifier_ `_`."},
    {text: "Você também pode declarar e inicializar um novo map na mesma linha com esta sintaxe."},
    {text: "O pacote `maps` contém várias funções utilitárias para maps."},
    {text: "Saída:\n# Note that maps appear in the form `map[k:v k:v]` when\n# printed with `fmt.Println`.\n$ go run maps.go \nmap: map[k1:7 k2:13]\nv1: 7\nv3: 0\nlen: 2\nmap: map[k1:7]\nmap: map[]\nprs: false\nmap: map[bar:2 foo:1]\nn == n2"}
  ]}
/>

<a href="https://go.dev/play/p/5jpkxJ2T0Lv" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
