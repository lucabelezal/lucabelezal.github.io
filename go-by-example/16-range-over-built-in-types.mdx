---
title: Range sobre Tipos Embutidos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Range sobre Tipos Embutidos"}
  code={`package main

import "fmt"

func main() {
    nums := []int{2, 3, 4}
    sum := 0
    for _, num := range nums {
        sum += num
    }
    fmt.Println("sum:", sum)
    for i, num := range nums {
        if num == 3 {
            fmt.Println("index:", i)
        }
    }
    for k := range kvs {
        fmt.Println("key:", k)
    }
    for i, c := range "go" {
        fmt.Println(i, c)
    }
}`}
  sections={[
    {text: "Aqui usamos `range` para somar números de um slice. Arrays funcionam igual."},
    {text: "`range` em arrays e slices fornece índice e valor. Acima não precisamos do índice, então ignoramos com `_`. Às vezes queremos os índices."},
    {text: "`range` também itera apenas sobre as chaves de um map."},
    {text: "`range` em strings itera sobre code points Unicode. O primeiro valor é o índice inicial do `rune` e o segundo é o próprio `rune`. Veja [Strings e Runes](strings-and-runes)."}
  ]}
/>

<a href="https://go.dev/play/p/S171w0PjgsD" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Pointers](/go/pointers).
