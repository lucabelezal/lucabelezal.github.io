---
title: Recover
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Recover"}
  code={`package main
import "fmt"
func mayPanic() {
    panic("a problem")
}
func main() {
    defer func() {
        if r := recover(); r != nil {
            fmt.Println("Recovered. Error:\
", r)
        }
    }()
    mayPanic()
    fmt.Println("After mayPanic()")
}`}
  sections={[
    {text: "This function panics."},
    {text: "recover must be called within a deferred function. When the enclosing function panics, the defer will activate and a recover call within it will catch the panic."},
    {text: "The return value of recover is the error raised in the call to panic."},
    {text: "This code will not run, because mayPanic panics. The execution of main stops at the point of the panic and resumes in the deferred closure."},
    {text: "Saída:\n$ go run recover.go\nRecovered. Error:\n a problem"}
  ]}
/>

<a href="https://go.dev/play/p/Sk-SVdofEIZ" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [String Functions](/go/string-functions).
