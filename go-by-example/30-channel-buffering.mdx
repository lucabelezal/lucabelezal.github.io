---
title: Buffer de Channels
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Buffer de Channels"}
  code={`package main

import "fmt"

func main() {
    messages := make(chan string, 2)
    messages <- "buffered"
    messages <- "channel"
    fmt.Println(<-messages)
    fmt.Println(<-messages)
}`}
  sections={[
    {text: "Aqui criamos um channel de strings com buffer para até 2 valores."},
    {text: "Como é bufferizado, podemos enviar valores sem um receive concorrente."},
    {text: "Depois podemos receber os dois valores normalmente."},
    {text: "Saída:\n$ go run channel-buffering.go \nbuffered\nchannel"}
  ]}
/>

<a href="https://go.dev/play/p/3BRCdRnRszb" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
