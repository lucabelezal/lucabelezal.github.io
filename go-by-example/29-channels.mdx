---
title: Channels
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Channels"}
  code={`package main

import "fmt"

func main() {
    messages := make(chan string)
    go func() { messages <- "ping" }()
    msg := <-messages
    fmt.Println(msg)
}`}
  sections={[
    {text: "Crie um channel com `make(chan val-type)`. Channels são tipados pelos valores que transportam."},
    {text: "_Envie_ um valor para um channel usando a sintaxe `channel <-`. Aqui enviamos `\"ping\"` para o channel `messages` criado acima, a partir de uma nova goroutine."},
    {text: "A sintaxe `<-channel` _recebe_ um valor do channel. Aqui vamos receber a mensagem `\"ping\"` que enviamos acima e imprimi-la."},
    {text: "Saída:\n# When we run the program the `\"ping\"` message is\n# successfully passed from one goroutine to another via\n# our channel.\n$ go run channels.go \nping\n\n# By default sends and receives block until both the\n# sender and receiver are ready. This property allowed\n# us to wait at the end of our program for the `\"ping\"`\n# message without having to use any other synchronization."}
  ]}
/>

<a href="https://go.dev/play/p/MaLY7AiAkHM" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Channel Buffering](/go/channel-buffering).
