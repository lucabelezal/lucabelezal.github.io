---
title: Channels
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Channels" playHash="MaLY7AiAkHM" next={{title: "Channel Buffering", href: "/go/channel-buffering"}}>

<GoExampleRow docs={"_Channels_ are the pipes that connect concurrent goroutines. You can send values into channels from one goroutine and receive those values into another goroutine."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"channels.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import \"fmt\""} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"Create a new channel with `make(chan val-type)`. Channels are typed by the values they convey."} language="go" code={"    messages := make(chan string)"} />

<GoExampleRow docs={"_Send_ a value into a channel using the `channel <-` syntax. Here we send `\"ping\"`  to the `messages` channel we made above, from a new goroutine."} language="go" code={"    go func() { messages <- \"ping\" }()"} />

<GoExampleRow docs={"The `<-channel` syntax _receives_ a value from the channel. Here we'll receive the `\"ping\"` message we sent above and print it out."} language="go" code={"    msg := <-messages\n    fmt.Println(msg)\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"# When we run the program the `\"ping\"` message is\n# successfully passed from one goroutine to another via\n# our channel.\n$ go run channels.go \nping\n\n# By default sends and receives block until both the\n# sender and receiver are ready. This property allowed\n# us to wait at the end of our program for the `\"ping\"`\n# message without having to use any other synchronization."} />

</GoExample>
