---
title: Ordenação
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Ordenação"}
  code={`package main

import (
    "fmt"
    "slices"
)

func main() {
    strs := []string{"c", "a", "b"}
    slices.Sort(strs)
    fmt.Println("Strings:", strs)
    ints := []int{7, 2, 4}
    slices.Sort(ints)
    fmt.Println("Ints:   ", ints)
    s := slices.IsSorted(ints)
    fmt.Println("Sorted: ", s)
}`}
  sections={[
    {text: "Funções de ordenação são genéricas e funcionam para qualquer tipo _ordenado_. Veja [cmp.Ordered](https://pkg.go.dev/cmp#Ordered)."},
    {text: "Um exemplo ordenando `int`s."},
    {text: "Também podemos usar `slices` para checar se o slice já está ordenado."},
    {text: "Saída:\n$ go run sorting.go\nStrings: [a b c]\nInts:    [2 4 7]\nSorted:  true"}
  ]}
/>

<a href="https://go.dev/play/p/X7iJcIua02T" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Sorting by Functions](/go/sorting-by-functions).
