---
title: URL Parsing
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="URL Parsing" playHash="fHTQn9X7l1B" next={{title: "SHA256 Hashes", href: "/go/sha256-hashes"}}>

<GoExampleRow docs={"URLs provide a [uniform way to locate resources](https://adam.herokuapp.com/past/2010/3/30/urls_are_the_uniform_way_to_locate_resources/). Here's how to parse URLs in Go."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"url-parsing.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"net\"\n    \"net/url\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"We'll parse this example URL, which includes a scheme, authentication info, host, port, path, query params, and query fragment."} language="go" code={"    s := \"postgres://user:pass@host.com:5432/path?k=v#f\""} />

<GoExampleRow docs={"Parse the URL and ensure there are no errors."} language="go" code={"    u, err := url.Parse(s)\n    if err != nil {\n        panic(err)\n    }"} />

<GoExampleRow docs={"Accessing the scheme is straightforward."} language="go" code={"    fmt.Println(u.Scheme)"} />

<GoExampleRow docs={"`User` contains all authentication info; call `Username` and `Password` on this for individual values."} language="go" code={"    fmt.Println(u.User)\n    fmt.Println(u.User.Username())\n    p, _ := u.User.Password()\n    fmt.Println(p)"} />

<GoExampleRow docs={"The `Host` contains both the hostname and the port, if present. Use `SplitHostPort` to extract them."} language="go" code={"    fmt.Println(u.Host)\n    host, port, _ := net.SplitHostPort(u.Host)\n    fmt.Println(host)\n    fmt.Println(port)"} />

<GoExampleRow docs={"Here we extract the `path` and the fragment after the `#`."} language="go" code={"    fmt.Println(u.Path)\n    fmt.Println(u.Fragment)"} />

<GoExampleRow docs={"To get query params in a string of `k=v` format, use `RawQuery`. You can also parse query params into a map. The parsed query param maps are from strings to slices of strings, so index into `[0]` if you only want the first value."} language="go" code={"    fmt.Println(u.RawQuery)\n    m, _ := url.ParseQuery(u.RawQuery)\n    fmt.Println(m)\n    fmt.Println(m[\"k\"][0])\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"# Running our URL parsing program shows all the different\n# pieces that we extracted.\n$ go run url-parsing.go \npostgres\nuser:pass\nuser\npass\nhost.com:5432\nhost.com\n5432\n/path\nf\nk=v\nmap[k:[v]]\nv"} />

</GoExample>
