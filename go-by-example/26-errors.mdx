---
title: Errors
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Errors" playHash="xWXAMmcrbTs" next={{title: "Custom Errors", href: "/go/custom-errors"}}>

<GoExampleRow docs={"In Go it's idiomatic to communicate errors via an explicit, separate return value. This contrasts with the exceptions used in languages like Java, Python and Ruby and the overloaded single result / error value sometimes used in C. Go's approach makes it easy to see which functions return errors and to handle them using the same language constructs employed for other, non-error tasks.  See the documentation of the [errors package](https://pkg.go.dev/errors) and [this blog post](https://go.dev/blog/go1.13-errors) for additional details."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"errors.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"errors\"\n    \"fmt\"\n)"} />

<GoExampleRow docs={"By convention, errors are the last return value and have type `error`, a built-in interface."} language="go" code={"func f(arg int) (int, error) {\n    if arg == 42 {"} />

<GoExampleRow docs={"`errors.New` constructs a basic `error` value with the given error message."} language="go" code={"        return -1, errors.New(\"can't work with 42\")\n    }"} />

<GoExampleRow docs={"A `nil` value in the error position indicates that there was no error."} language="go" code={"    return arg + 3, nil\n}"} />

<GoExampleRow docs={"A sentinel error is a predeclared variable that is used to signify a specific error condition."} language="go" code={"var ErrOutOfTea = errors.New(\"no more tea available\")\nvar ErrPower = errors.New(\"can't boil water\")"} />

<GoExampleRow docs={undefined} language="go" code={"func makeTea(arg int) error {\n    if arg == 2 {\n        return ErrOutOfTea\n    } else if arg == 4 {"} />

<GoExampleRow docs={"We can wrap errors with higher-level errors to add context. The simplest way to do this is with the `%w` verb in `fmt.Errorf`. Wrapped errors create a logical chain (A wraps B, which wraps C, etc.) that can be queried with functions like `errors.Is` and `errors.AsType`."} language="go" code={"        return fmt.Errorf(\"making tea: %w\", ErrPower)\n    }\n    return nil\n}"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {\n    for _, i := range []int{7, 42} {"} />

<GoExampleRow docs={"It's idiomatic to use an inline error check in the `if` line."} language="go" code={"        if r, e := f(i); e != nil {\n            fmt.Println(\"f failed:\", e)\n        } else {\n            fmt.Println(\"f worked:\", r)\n        }\n    }"} />

<GoExampleRow docs={undefined} language="go" code={"    for i := range 5 {\n        if err := makeTea(i); err != nil {"} />

<GoExampleRow docs={"`errors.Is` checks that a given error (or any error in its chain) matches a specific error value. This is especially useful with wrapped or nested errors, allowing you to identify specific error types or sentinel errors in a chain of errors."} language="go" code={"            if errors.Is(err, ErrOutOfTea) {\n                fmt.Println(\"We should buy new tea!\")\n            } else if errors.Is(err, ErrPower) {\n                fmt.Println(\"Now it is dark.\")\n            } else {\n                fmt.Printf(\"unknown error: %s\\n\", err)\n            }\n            continue\n        }"} />

<GoExampleRow docs={undefined} language="go" code={"        fmt.Println(\"Tea is ready!\")\n    }\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go run errors.go\nf worked: 10\nf failed: can't work with 42\nTea is ready!\nTea is ready!\nWe should buy new tea!\nTea is ready!\nNow it is dark."} />

</GoExample>
