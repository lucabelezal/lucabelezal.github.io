---
title: Expressões Regulares
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Expressões Regulares"}
  code={`package main

import (
    "bytes"
    "fmt"
    "regexp"
)

func main() {
    match, _ := regexp.MatchString("p([a-z]+)ch", "peach")
    fmt.Println(match)
    r, _ := regexp.Compile("p([a-z]+)ch")
    fmt.Println(r.MatchString("peach"))
    fmt.Println(r.FindString("peach punch"))
    fmt.Println("idx:", r.FindStringIndex("peach punch"))
    fmt.Println(r.FindStringSubmatch("peach punch"))
    fmt.Println(r.FindStringSubmatchIndex("peach punch"))
    fmt.Println(r.FindAllString("peach punch pinch", -1))
    fmt.Println("all:", r.FindAllStringSubmatchIndex(
        "peach punch pinch", -1))
    fmt.Println(r.FindAllString("peach punch pinch", 2))
    fmt.Println(r.Match([]byte("peach")))
    r = regexp.MustCompile("p([a-z]+)ch")
    fmt.Println("regexp:", r)
    in := []byte("a peach")
    out := r.ReplaceAllFunc(in, bytes.ToUpper)
    fmt.Println(string(out))
}`}
  sections={[
    {text: "Isso testa se um padrão casa com uma string."},
    {text: "Acima usamos padrão direto, mas para outras tarefas precisa compilar com `Compile` um `Regexp` otimizado."},
    {text: "Muitos métodos estão disponíveis. Aqui um teste de match como antes."},
    {text: "Isso encontra o match da regexp."},
    {text: "Também encontra o primeiro match, mas retorna índices de início e fim em vez do texto."},
    {text: "Variantes `Submatch` incluem o match completo e submatches. Por exemplo, retorna info para `p([a-z]+)ch` e `([a-z]+)`."},
    {text: "Similarmente, isso retorna índices de matches e submatches."},
    {text: "Variantes `All` aplicam a todos os matches, não só o primeiro. Por exemplo, encontrar todos os matches."},
    {text: "Essas variantes `All` também existem para as outras funções vistas."},
    {text: "Passar inteiro não-negativo como segundo argumento limita o número de matches."},
    {text: "Exemplos acima usaram strings e nomes como `MatchString`. Também pode passar `[]byte` e remover `String` do nome."},
    {text: "Para variáveis globais com regex, use `MustCompile`. Ele dá panic em vez de erro, mais seguro para globais."},
    {text: "A variante `Func` permite transformar texto casado com uma função."}
  ]}
/>

<a href="https://go.dev/play/p/fI2YIfYsCaL" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [JSON](/go/json).
