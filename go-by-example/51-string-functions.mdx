---
title: Funções de Strings
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Funções de Strings"}
  code={`package main

import (
    "fmt"
    s "strings"
)

var p = fmt.Println

func main() {
    p("Contains:  ", s.Contains("test", "es"))
    p("Count:     ", s.Count("test", "t"))
    p("HasPrefix: ", s.HasPrefix("test", "te"))
    p("HasSuffix: ", s.HasSuffix("test", "st"))
    p("Index:     ", s.Index("test", "e"))
    p("Join:      ", s.Join([]string{"a", "b"}, "-"))
    p("Repeat:    ", s.Repeat("a", 5))
    p("Replace:   ", s.Replace("foo", "o", "0", -1))
    p("Replace:   ", s.Replace("foo", "o", "0", 1))
    p("Split:     ", s.Split("a-b-c-d-e", "-"))
    p("ToLower:   ", s.ToLower("TEST"))
    p("ToUpper:   ", s.ToUpper("test"))
}`}
  sections={[
    {text: "Apelidamos `fmt.Println` para um nome mais curto, pois usaremos bastante abaixo."},
    {text: "Aqui uma amostra das funções em `strings`. Como são funções do pacote, não métodos da string, passamos a string como primeiro argumento. Veja mais em [`strings`](https://pkg.go.dev/strings)age docs."},
    {text: "Saída:\n$ go run string-functions.go\nContains:   true\nCount:      2\nHasPrefix:  true\nHasSuffix:  true\nIndex:      1\nJoin:       a-b\nRepeat:     aaaaa\nReplace:    f00\nReplace:    f0o\nSplit:      [a b c d e]\nToLower:    test\nToUpper:    TEST"}
  ]}
/>

<a href="https://go.dev/play/p/wKSAzxfs96O" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [String Formatting](/go/string-formatting).
