---
title: Parsing de Números
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Parsing de Números"}
  code={`package main

import (
    "fmt"
    "strconv"
)

func main() {
    f, _ := strconv.ParseFloat("1.234", 64)
    fmt.Println(f)
    i, _ := strconv.ParseInt("123", 0, 64)
    fmt.Println(i)
    d, _ := strconv.ParseInt("0x1c8", 0, 64)
    fmt.Println(d)
    u, _ := strconv.ParseUint("789", 0, 64)
    fmt.Println(u)
    k, _ := strconv.Atoi("135")
    fmt.Println(k)
    _, e := strconv.Atoi("wat")
    fmt.Println(e)
}`}
  sections={[
    {text: "O pacote `strconv` faz parsing de números."},
    {text: "Em `ParseFloat`, o `64` indica bits de precisão."},
    {text: "Em `ParseInt`, `0` infere a base da string. `64` exige resultado em 64 bits."},
    {text: "`ParseInt` reconhece números em hex."},
    {text: "`ParseUint` também está disponível."},
    {text: "`Atoi` é conveniência para parsing base 10 de `int`."},
    {text: "Funções de parsing retornam erro em input inválido."},
    {text: "Saída:\n$ go run number-parsing.go \n1.234\n123\n456\n789\n135\nstrconv.ParseInt: parsing \"wat\": invalid syntax\n\n# Next we'll look at another common parsing task: URLs."}
  ]}
/>

<a href="https://go.dev/play/p/ZAMEid6Fpmu" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
