---
title: Closures
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Closures"}
  code={`package main

import "fmt"

func intSeq() func() int {
    i := 0
    return func() int {
        i++
        return i
    }
}
func main() {
    nextInt := intSeq()
    fmt.Println(nextInt())
    fmt.Println(nextInt())
    fmt.Println(nextInt())
    newInts := intSeq()
    fmt.Println(newInts())
}`}
  sections={[
    {text: "A função `intSeq` retorna outra função, definida anonimamente no corpo de `intSeq`. A função retornada faz _closure_ sobre `i`."},
    {text: "Chamamos `intSeq` e atribuímos o resultado (uma função) a `nextInt`. Essa função captura seu próprio `i`, atualizado a cada chamada."},
    {text: "Veja o efeito do closure chamando `nextInt` algumas vezes."},
    {text: "Para confirmar que o estado é único daquela função, crie e teste uma nova."},
    {text: "Saída:\n$ go run closures.go\n1\n2\n3\n1\n\n# The last feature of functions we'll look at for now is\n# recursion."}
  ]}
/>

<a href="https://go.dev/play/p/NpgpzS8ZG8y" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
