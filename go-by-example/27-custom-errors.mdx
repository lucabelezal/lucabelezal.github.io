---
title: Erros Customizados
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Erros Customizados"}
  code={`package main

import (
    "errors"
    "fmt"
)

type argError struct {
    arg     int
    message string
}

func (e *argError) Error() string {
    return fmt.Sprintf("%d - %s", e.arg, e.message)
}
func f(arg int) (int, error) {
    if arg == 42 {
        return -1, &argError{arg, "can't work with it"}
    }
    return arg + 3, nil
}
func main() {
    _, err := f(42)
    if ae, ok := errors.AsType[*argError](err); ok {
        fmt.Println(ae.arg)
        fmt.Println(ae.message)
    } else {
        fmt.Println("err doesn't match argError")
    }
}`}
  sections={[
    {text: "Um tipo de erro customizado geralmente tem sufixo \"Error\"."},
    {text: "Adicionar o método `Error` faz `argError` implementar a interface `error`."},
    {text: "Retorna nosso erro customizado."},
    {text: "`errors.As` é uma versão mais avançada de `errors.Is`. Verifica se um erro na cadeia corresponde a um tipo e converte, retornando `true`. Sem match, retorna `false`."},
    {text: "Saída:\n$ go run custom-errors.go\n42\ncan't work with it"}
  ]}
/>

<a href="https://go.dev/play/p/0WHh8JFOjQq" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
