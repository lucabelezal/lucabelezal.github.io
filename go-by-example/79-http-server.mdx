---
title: Servidor HTTP
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Servidor HTTP"}
  code={`package main

import (
    "fmt"
    "net/http"
)

func hello(w http.ResponseWriter, req *http.Request) {
    fmt.Fprintf(w, "hello\n")
}
func headers(w http.ResponseWriter, req *http.Request) {
    for name, headers := range req.Header {
        for _, h := range headers {
            fmt.Fprintf(w, "%v: %v\n", name, h)
        }
    }
}
func main() {
    http.HandleFunc("/hello", hello)
    http.HandleFunc("/headers", headers)
    http.ListenAndServe(":8090", nil)
}`}
  sections={[
    {text: "Escrever um servidor HTTP básico é fácil com `net/http`."},
    {text: "Conceito fundamental em servidores `net/http` são *handlers*. Um handler implementa `http.Handler`. Forma comum é usar `http.HandlerFunc` em funções com assinatura apropriada."},
    {text: "Functions serving as handlers take a `http.ResponseWriter` and a `http.Request` as arguments. The response writer is used to fill in the HTTP response. Here our simple response is just \"hello\\\n\"."},
    {text: "Este handler lê todos os headers e os ecoa no body."},
    {text: "Registramos handlers nas rotas com `http.HandleFunc`. Configura o *default router* e recebe função como argumento."},
    {text: "Por fim chamamos `ListenAndServe` com porta e handler. `nil` usa o default router configurado."},
    {text: "Saída:\n# Run the server in the background.\n$ go run http-server.go &\n\n# Access the `/hello` route.\n$ curl localhost:8090/hello\nhello"}
  ]}
/>

<a href="https://go.dev/play/p/s3xMMt9Ytry" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [TCP Server](/go/tcp-server).
