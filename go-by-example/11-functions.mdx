---
title: Funções
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Funções"}
  code={`package main

import "fmt"

func plus(a int, b int) int {
    return a + b
}
func plusPlus(a, b, c int) int {
    return a + b + c
}
func main() {
    res := plus(1, 2)
    fmt.Println("1+2 =", res)
    res = plusPlus(1, 2, 3)
    fmt.Println("1+2+3 =", res)
}`}
  sections={[
    {text: "Uma função que recebe dois `int`s e retorna a soma como `int`."},
    {text: "Go exige `return` explícito, não retorna automaticamente o valor da última expressão."},
    {text: "Quando há múltiplos parâmetros consecutivos do mesmo tipo, pode omitir o tipo até o último que declara o tipo."},
    {text: "Chame a função como esperado, com `name(args)`."},
    {text: "Saída:\n$ go run functions.go \n1+2 = 3\n1+2+3 = 6\n\n# There are several other features to Go functions. One is\n# multiple return values, which we'll look at next."}
  ]}
/>

<a href="https://go.dev/play/p/-o49-dQfGbK" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
