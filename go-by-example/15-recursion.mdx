---
title: Recursão
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Recursão"}
  code={`package main

import "fmt"

func fact(n int) int {
    if n == 0 {
        return 1
    }
    return n * fact(n-1)
}
func main() {
    fmt.Println(fact(7))
    var fib func(n int) int
    fib = func(n int) int {
        if n < 2 {
            return n
        }
        return fib(n-1) + fib(n-2)
    }
    fmt.Println(fib(7))
}`}
  sections={[
    {text: "A função `fact` chama a si mesma até o caso base `fact(0)`."},
    {text: "Funções anônimas também podem ser recursivas, mas exigem declarar a variável com `var` antes de defini-la."},
    {text: "Como `fib` foi previamente declarado em `main`, Go sabe qual função chamar aqui."},
    {text: "Saída:\n$ go run recursion.go \n5040\n13"}
  ]}
/>

<a href="https://go.dev/play/p/k4IRATLn9cE" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Range over Built-in Types](/go/range-over-built-in-types).
