---
title: Formatação de Strings
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Formatação de Strings"}
  code={`package main

import (
    "fmt"
    "os"
)

type point struct {
    x, y int
}

func main() {
    p := point{1, 2}
    fmt.Printf("struct1: %v\n", p)
    fmt.Printf("struct2: %+v\n", p)
    fmt.Printf("struct3: %#v\n", p)
    fmt.Printf("type: %T\n", p)
    fmt.Printf("bool: %t\n", true)
    fmt.Printf("int: %d\n", 123)
    fmt.Printf("bin: %b\n", 14)
    fmt.Printf("char: %c\n", 33)
    fmt.Printf("hex: %x\n", 456)
    fmt.Printf("float1: %f\n", 78.9)
    fmt.Printf("float2: %e\n", 123400000.0)
    fmt.Printf("float3: %E\n", 123400000.0)
    fmt.Printf("str1: %s\n", "\"string\"")
    fmt.Printf("str2: %q\n", "\"string\"")
    fmt.Printf("str3: %x\n", "hex this")
    fmt.Printf("pointer: %p\n", &p)
    fmt.Printf("width1: |%6d|%6d|\n", 12, 345)
    fmt.Printf("width2: |%6.2f|%6.2f|\n", 1.2, 3.45)
    fmt.Printf("width3: |%-6.2f|%-6.2f|\n", 1.2, 3.45)
    fmt.Printf("width4: |%6s|%6s|\n", "foo", "b")
    fmt.Printf("width5: |%-6s|%-6s|\n", "foo", "b")
    s := fmt.Sprintf("sprintf: a %s", "string")
    fmt.Println(s)
    fmt.Fprintf(os.Stderr, "io: an %s\n", "error")
}`}
  sections={[
    {text: "Go offers several printing \"verbs\" designed to format general Go values. For example, this prints an instance of our `point` struct."},
    {text: "Se o valor é struct, `%+v` inclui os nomes dos campos."},
    {text: "`%#v` imprime a representação em sintaxe Go, o trecho de código que geraria o valor."},
    {text: "Para imprimir o tipo, use `%T`."},
    {text: "Formatar booleanos é direto."},
    {text: "Há muitas opções para inteiros. Use `%d` para decimal base 10."},
    {text: "Isso imprime representação binária."},
    {text: "Isso imprime o caractere correspondente ao inteiro."},
    {text: "`%x` gera encoding hex."},
    {text: "Há várias opções para floats. Para decimal básico use `%f`."},
    {text: "`%e` e `%E` formatam em notação científica (versões levemente diferentes)."},
    {text: "Para imprimir string básica use `%s`."},
    {text: "Para strings com aspas duplas como no código Go, use `%q`."},
    {text: "Como em inteiros, `%x` renderiza a string em base 16, com dois caracteres por byte."},
    {text: "Para imprimir um ponteiro, use `%p`."},
    {text: "Ao formatar números, você quer controlar largura e precisão. Para largura de inteiro, use número após `%`. Por padrão, justificado à direita com espaços."},
    {text: "Também pode especificar largura de floats, geralmente restringindo precisão com `largura.precisao`."},
    {text: "Para justificar à esquerda, use o flag `-`."},
    {text: "Também pode controlar largura em strings, para alinhar em saída tabular. Para largura básica justificada à direita."},
    {text: "Para justificar à esquerda use `-` como em números."},
    {text: "Vimos `Printf`, que imprime em `os.Stdout`. `Sprintf` formata e retorna a string sem imprimir."},
    {text: "Você pode formatar e imprimir em `io.Writer`s diferentes de `os.Stdout` com `Fprintf`."},
    {text: "Saída:\n$ go run string-formatting.go\nstruct1: {1 2}\nstruct2: {x:1 y:2}\nstruct3: main.point{x:1, y:2}\ntype: main.point\nbool: true\nint: 123\nbin: 1110\nchar: !\nhex: 1c8\nfloat1: 78.900000\nfloat2: 1.234000e+08\nfloat3: 1.234000E+08\nstr1: \"string\"\nstr2: \"\\\"string\\\"\"\nstr3: 6865782074686973\npointer: 0xc0000ba000\nwidth1: |    12|   345|\nwidth2: |  1.20|  3.45|\nwidth3: |1.20  |3.45  |\nwidth4: |   foo|     b|\nwidth5: |foo   |b     |\nsprintf: a string\nio: an error"}
  ]}
/>

<a href="https://go.dev/play/p/EZCZX1Uwp6D" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
