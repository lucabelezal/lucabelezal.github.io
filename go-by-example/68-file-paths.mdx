---
title: Caminhos de Arquivos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Caminhos de Arquivos"}
  code={`package main

import (
    "fmt"
    "path/filepath"
    "strings"
)

func main() {
    p := filepath.Join("dir1", "dir2", "filename")
    fmt.Println("p:", p)
    fmt.Println(filepath.Join("dir1//", "filename"))
    fmt.Println(filepath.Join("dir1/../dir1", "filename"))
    fmt.Println("Dir(p):", filepath.Dir(p))
    fmt.Println("Base(p):", filepath.Base(p))
    fmt.Println(filepath.IsAbs("dir/file"))
    fmt.Println(filepath.IsAbs("/dir/file"))
    filename := "config.json"
    ext := filepath.Ext(filename)
    fmt.Println(ext)
    fmt.Println(strings.TrimSuffix(filename, ext))
    rel, err := filepath.Rel("a/b", "a/b/t/file")
    if err != nil {
        panic(err)
    }
    fmt.Println(rel)
    rel, err = filepath.Rel("a/b", "a/c/t/file")
    if err != nil {
        panic(err)
    }
    fmt.Println(rel)
}`}
  sections={[
    {text: "O pacote `filepath` fornece funções para analisar e construir *caminhos de arquivo* de forma portável entre sistemas; `dir/file` no Linux vs. `dir\\file` no Windows, por exemplo."},
    {text: "`Join` deve ser usado para construir paths portáveis. Recebe qualquer número de args e constrói path hierárquico."},
    {text: "Use sempre `Join` em vez de concatenar `/` ou `\\` manualmente. Além de garantir portabilidade, `Join` normaliza o path removendo separadores supérfluos e navegações de diretório."},
    {text: "`Dir` e `Base` separam path em diretório e arquivo. `Split` retorna ambos na mesma chamada."},
    {text: "Podemos checar se um path é absoluto."},
    {text: "Alguns arquivos têm extensão após ponto. Use `Ext` para extrair."},
    {text: "Para o nome sem extensão, use `strings.TrimSuffix`."},
    {text: "`Rel` encontra path relativo entre *base* e *target*. Retorna erro se não puder tornar relativo."},
    {text: "Saída:\n$ go run file-paths.go\np: dir1/dir2/filename\ndir1/filename\ndir1/filename\nDir(p): dir1/dir2\nBase(p): filename\nfalse\ntrue\n.json\nconfig\nt/file\n../c/t/file"}
  ]}
/>

<a href="https://go.dev/play/p/5h3lUytvmyO" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
