---
title: File Paths
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="File Paths" playHash="5h3lUytvmyO" next={{title: "Directories", href: "/go/directories"}}>

<GoExampleRow docs={"The `filepath` package provides functions to parse and construct *file paths* in a way that is portable between operating systems; `dir/file` on Linux vs. `dir\\file` on Windows, for example."} language="go" title={"file-paths.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"path/filepath\"\n    \"strings\"\n)"} />

<GoExampleRow docs={undefined} language="go" code={"func main() {"} />

<GoExampleRow docs={"`Join` should be used to construct paths in a portable way. It takes any number of arguments and constructs a hierarchical path from them."} language="go" code={"    p := filepath.Join(\"dir1\", \"dir2\", \"filename\")\n    fmt.Println(\"p:\", p)"} />

<GoExampleRow docs={"You should always use `Join` instead of concatenating `/`s or `\\`s manually. In addition to providing portability, `Join` will also normalize paths by removing superfluous separators and directory changes."} language="go" code={"    fmt.Println(filepath.Join(\"dir1//\", \"filename\"))\n    fmt.Println(filepath.Join(\"dir1/../dir1\", \"filename\"))"} />

<GoExampleRow docs={"`Dir` and `Base` can be used to split a path to the directory and the file. Alternatively, `Split` will return both in the same call."} language="go" code={"    fmt.Println(\"Dir(p):\", filepath.Dir(p))\n    fmt.Println(\"Base(p):\", filepath.Base(p))"} />

<GoExampleRow docs={"We can check whether a path is absolute."} language="go" code={"    fmt.Println(filepath.IsAbs(\"dir/file\"))\n    fmt.Println(filepath.IsAbs(\"/dir/file\"))"} />

<GoExampleRow docs={undefined} language="go" code={"    filename := \"config.json\""} />

<GoExampleRow docs={"Some file names have extensions following a dot. We can split the extension out of such names with `Ext`."} language="go" code={"    ext := filepath.Ext(filename)\n    fmt.Println(ext)"} />

<GoExampleRow docs={"To find the file's name with the extension removed, use `strings.TrimSuffix`."} language="go" code={"    fmt.Println(strings.TrimSuffix(filename, ext))"} />

<GoExampleRow docs={"`Rel` finds a relative path between a *base* and a *target*. It returns an error if the target cannot be made relative to base."} language="go" code={"    rel, err := filepath.Rel(\"a/b\", \"a/b/t/file\")\n    if err != nil {\n        panic(err)\n    }\n    fmt.Println(rel)"} />

<GoExampleRow docs={undefined} language="go" code={"    rel, err = filepath.Rel(\"a/b\", \"a/c/t/file\")\n    if err != nil {\n        panic(err)\n    }\n    fmt.Println(rel)\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go run file-paths.go\np: dir1/dir2/filename\ndir1/filename\ndir1/filename\nDir(p): dir1/dir2\nBase(p): filename\nfalse\ntrue\n.json\nconfig\nt/file\n../c/t/file"} />

</GoExample>
