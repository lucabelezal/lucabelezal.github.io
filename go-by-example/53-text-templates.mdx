---
title: Templates de Texto
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Templates de Texto"}
  code={`package main

import (
    "os"
    "text/template"
)

func main() {
    t1 := template.New("t1")
    t1, err := t1.Parse("Value is {{.}}\n")
    if err != nil {
        panic(err)
    }
    t1 = template.Must(t1.Parse("Value: {{.}}\n"))
    t1.Execute(os.Stdout, "some text")
    t1.Execute(os.Stdout, 5)
    t1.Execute(os.Stdout, []string{
        "Go",
        "Rust",
        "C++",
        "C#",
    })
    Create := func(name, t string) *template.Template {
        return template.Must(template.New(name).Parse(t))
    }
    t2 := Create("t2", "Name: {{.Name}}\n")
    t2.Execute(os.Stdout, struct {
        Name string
    }{"Jane Doe"})
    t2.Execute(os.Stdout, map[string]string{
        "Name": "Mickey Mouse",
    })
    t3 := Create("t3",
        "{{if . -}} yes {{else -}} no {{end}}\n")
    t3.Execute(os.Stdout, "not empty")
    t3.Execute(os.Stdout, "")
    t4 := Create("t4",
        "Range: {{range .}}{{.}} {{end}}\n")
    t4.Execute(os.Stdout,
        []string{
            "Go",
            "Rust",
            "C++",
            "C#",
        })
}`}
  sections={[
    {text: "Podemos criar um novo template e fazer parse do corpo a partir de string. Templates misturam texto estático e \"actions\" em `{{..."},
    {text: "Alternativamente, use `template.Must` para panic caso `Parse` retorne erro. Útil para templates globais."},
    {text: "Ao \"executar\" o template geramos o texto com valores específicos. O `{{."},
    {text: "Função auxiliar usada abaixo."},
    {text: "Se o dado é struct, usamos `{{.FieldName"},
    {text: "O mesmo vale para maps; sem restrição de caixa nos nomes das chaves."},
    {text: "Blocos range permitem iterar slices, arrays, maps ou channels. Dentro do bloco `{{."},
    {text: "Saída:\n$ go run templates.go \nValue: some text\nValue: 5\nValue: [Go Rust C++ C#]\nName: Jane Doe\nName: Mickey Mouse\nyes \nno \nRange: Go Rust C++ C#"}
  ]}
/>

<a href="https://go.dev/play/p/pDwkw1iMACF" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
