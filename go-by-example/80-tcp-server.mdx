---
title: Servidor TCP
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Servidor TCP"}
  code={`package main

import (
    "bufio"
    "fmt"
    "log"
    "net"
    "strings"
)

func main() {
    listener, err := net.Listen("tcp", ":8090")
    if err != nil {
        log.Fatal("Error listening:", err)
    }
    defer listener.Close()
    for {
        conn, err := listener.Accept()
        if err != nil {
            log.Println("Error accepting conn:", err)
            continue
        }
        go handleConnection(conn)
    }
}
func handleConnection(conn net.Conn) {
    defer conn.Close()
    reader := bufio.NewReader(conn)
    message, err := reader.ReadString('\n')
    if err != nil {
        log.Printf("Read error: %v", err)
        return
    }
    ackMsg := strings.ToUpper(strings.TrimSpace(message))
    response := fmt.Sprintf("ACK: %s\n", ackMsg)
    _, err = conn.Write([]byte(response))
    if err != nil {
        log.Printf("Server write error: %v", err)
    }
}`}
  sections={[
    {text: "O pacote `net` fornece ferramentas para construir servidores TCP facilmente."},
    {text: "`net.Listen` inicia o servidor na rede (TCP) e endereço (porta 8090 em todas as interfaces)."},
    {text: "Feche o listener para liberar a porta ao sair."},
    {text: "Loop infinito para aceitar novas conexões."},
    {text: "Aguarde uma conexão."},
    {text: "Usamos goroutine para tratar a conexão e manter o loop aceitando novas."},
    {text: "`handleConnection` trata uma conexão, lendo uma linha do cliente e retornando resposta."},
    {text: "Fechar a conexão libera recursos ao terminar."},
    {text: "Use `bufio.NewReader` para ler uma linha do cliente (terminada em newline)."},
    {text: "Crie e envie resposta ao cliente, demonstrando comunicação bidirecional."},
    {text: "Saída:\n# Run the TCP server in the background.\n$ go run tcp-server.go &\n\n# Send data and capture the response using netcat.\n$ echo \"Hello from netcat\" | nc localhost 8090\nACK: HELLO FROM NETCAT"}
  ]}
/>

<a href="https://go.dev/play/p/G0k2jHMA6ei" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Context](/go/context).
