---
title: Argumentos de Linha de Comando
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Argumentos de Linha de Comando"}
  code={`package main

import (
    "fmt"
    "os"
)

func main() {
    argsWithProg := os.Args
    argsWithoutProg := os.Args[1:]
    arg := os.Args[3]
    fmt.Println(argsWithProg)
    fmt.Println(argsWithoutProg)
    fmt.Println(arg)
}`}
  sections={[
    {text: "`os.Args` dá acesso aos argumentos brutos. O primeiro valor é o path do programa, e `os.Args[1:]` são os argumentos."},
    {text: "Você pode pegar args individuais com indexação normal."},
    {text: "Saída:\n# To experiment with command-line arguments it's best to\n# build a binary with `go build` first.\n$ go build command-line-arguments.go\n$ ./command-line-arguments a b c d\n[./command-line-arguments a b c d]       \n[a b c d]\nc\n\n# Next we'll look at more advanced command-line processing\n# with flags."}
  ]}
/>

<a href="https://go.dev/play/p/UYCEvh9d2Zb" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
