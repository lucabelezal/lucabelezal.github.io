---
title: WaitGroups
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"WaitGroups"}
  code={`package main

import (
    "fmt"
    "sync"
    "time"
)

func worker(id int) {
    fmt.Printf("Worker %d starting\n", id)
    time.Sleep(time.Second)
    fmt.Printf("Worker %d done\n", id)
}
func main() {
    var wg sync.WaitGroup
    for i := 1; i <= 5; i++ {
        wg.Go(func() {
            worker(i)
        })
    }
    wg.Wait()
}`}
  sections={[
    {text: "Esta é a função que rodaremos em cada goroutine."},
    {text: "Sleep para simular tarefa custosa."},
    {text: "Este WaitGroup espera todas as goroutines terminarem. Nota: se passar WaitGroup para funções, passe *por ponteiro*."},
    {text: "Dispare várias goroutines usando `WaitGroup.Go`"},
    {text: "Bloqueie até todas as goroutines de `wg` terminarem. Uma goroutine termina quando sua função retorna."},
    {text: "Note que esta abordagem não propaga erros facilmente. Para casos avançados, considere [errgroup](https://pkg.go.dev/golang.org/x/sync/errgroup)."},
    {text: "Saída:\n$ go run waitgroups.go\nWorker 5 starting\nWorker 3 starting\nWorker 4 starting\nWorker 1 starting\nWorker 2 starting\nWorker 4 done\nWorker 1 done\nWorker 2 done\nWorker 5 done\nWorker 3 done\n\n# The order of workers starting up and finishing\n# is likely to be different for each invocation."}
  ]}
/>

<a href="https://go.dev/play/p/csaELahJTWt" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Rate Limiting](/go/rate-limiting).
