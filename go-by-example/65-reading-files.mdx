---
title: Lendo Arquivos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Lendo Arquivos"}
  code={`package main

import (
    "bufio"
    "fmt"
    "io"
    "os"
    "path/filepath"
)

func check(e error) {
    if e != nil {
        panic(e)
    }
}
func main() {
    path := filepath.Join(os.TempDir(), "dat")
    dat, err := os.ReadFile(path)
    check(err)
    fmt.Print(string(dat))
    f, err := os.Open(path)
    check(err)
    b1 := make([]byte, 5)
    n1, err := f.Read(b1)
    check(err)
    fmt.Printf("%d bytes: %s\n", n1, string(b1[:n1]))
    o2, err := f.Seek(6, io.SeekStart)
    check(err)
    b2 := make([]byte, 2)
    n2, err := f.Read(b2)
    check(err)
    fmt.Printf("%d bytes @ %d: ", n2, o2)
    fmt.Printf("%v\n", string(b2[:n2]))
    _, err = f.Seek(2, io.SeekCurrent)
    check(err)
    _, err = f.Seek(-4, io.SeekEnd)
    check(err)
    o3, err := f.Seek(6, io.SeekStart)
    check(err)
    b3 := make([]byte, 2)
    n3, err := io.ReadAtLeast(f, b3, 2)
    check(err)
    fmt.Printf("%d bytes @ %d: %s\n", n3, o3, string(b3))
    _, err = f.Seek(0, io.SeekStart)
    check(err)
    r4 := bufio.NewReader(f)
    b4, err := r4.Peek(5)
    check(err)
    fmt.Printf("5 bytes: %s\n", string(b4))
    f.Close()
}`}
  sections={[
    {text: "Ler arquivos exige checar erros na maioria das chamadas. Este helper simplifica as checagens."},
    {text: "A tarefa mais básica é ler todo o conteúdo do arquivo para memória."},
    {text: "Frequentemente quer mais controle sobre o que ler. Para isso, faça `Open` para obter um `os.File`."},
    {text: "Leia alguns bytes do início. Permita até 5, mas note quantos realmente foram lidos."},
    {text: "Também pode dar `Seek` para uma posição conhecida e `Read` a partir dela."},
    {text: "Outros seeks são relativos à posição atual,"},
    {text: "e relativos ao fim do arquivo."},
    {text: "O pacote `io` fornece funções úteis para leitura. Por exemplo, leituras como acima ficam mais robustas com `ReadAtLeast`."},
    {text: "Não há rewind embutido, mas `Seek(0, io.SeekStart)` faz isso."},
    {text: "`bufio` implementa reader bufferizado, útil por eficiência com muitas leituras pequenas e por métodos adicionais."},
    {text: "Feche o arquivo ao terminar (geralmente agendado logo após `Open` com `defer`)."}
  ]}
/>

<a href="https://go.dev/play/p/J710-KJyC8z" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
