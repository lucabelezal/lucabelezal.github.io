---
title: Constants
---

import GoExample, {GoExampleRow} from '@site/src/components/GoExample';

<GoExample title="Constants" playHash="LfvIxHlpomp" next={{title: "For", href: "/go/for"}}>

<GoExampleRow docs={"Go supports _constants_ of character, string, boolean, and numeric values."} empty code="" />

<GoExampleRow docs={undefined} language="go" title={"constants.go"} code={"package main"} />

<GoExampleRow docs={undefined} language="go" code={"import (\n    \"fmt\"\n    \"math\"\n)"} />

<GoExampleRow docs={"`const` declares a constant value."} language="go" code={"const s string = \"constant\""} />

<GoExampleRow docs={undefined} language="go" code={"func main() {\n    fmt.Println(s)"} />

<GoExampleRow docs={"A `const` statement can also appear inside a function body."} language="go" code={"    const n = 500000000"} />

<GoExampleRow docs={"Constant expressions perform arithmetic with arbitrary precision."} language="go" code={"    const d = 3e20 / n\n    fmt.Println(d)"} />

<GoExampleRow docs={"A numeric constant has no type until it's given one, such as by an explicit conversion."} language="go" code={"    fmt.Println(int64(d))"} />

<GoExampleRow docs={"A number can be given a type by using it in a context that requires one, such as a variable assignment or function call. For example, here `math.Sin` expects a `float64`."} language="go" code={"    fmt.Println(math.Sin(n))\n}"} />

<GoExampleRow docs={"Saída:"} language="bash" code={"$ go run constant.go \nconstant\n6e+11\n600000000000\n-0.28470407323754404"} />

</GoExample>
