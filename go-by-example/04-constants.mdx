---
title: Constantes
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Constantes"}
  code={`package main

import (
    "fmt"
    "math"
)

const s string = "constant"

func main() {
    fmt.Println(s)
    const n = 500000000
    const d = 3e20 / n
    fmt.Println(d)
    fmt.Println(int64(d))
    fmt.Println(math.Sin(n))
}`}
  sections={[
    {text: "`const` declara um valor constante."},
    {text: "Uma declaração `const` também pode aparecer dentro de uma função."},
    {text: "Expressões com constantes fazem aritmética com precisão arbitrária."},
    {text: "Uma constante numérica não tem tipo até receber um, por exemplo via conversão explícita."},
    {text: "Um número pode receber um tipo ao ser usado em um contexto que exige um, como atribuição a variável ou chamada de função. Por exemplo, aqui `math.Sin` espera um `float64`."},
    {text: "Saída:\n$ go run constant.go \nconstant\n6e+11\n600000000000\n-0.28470407323754404"}
  ]}
/>

<a href="https://go.dev/play/p/LfvIxHlpomp" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [For](/go/for).
