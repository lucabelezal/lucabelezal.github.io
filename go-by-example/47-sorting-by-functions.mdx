---
title: Sorting by Functions
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Sorting by Functions"}
  code={`package main
import (
    "cmp"
    "fmt"
    "slices"
)
func main() {
    fruits := []string{"peach", "banana", "kiwi"}
    lenCmp := func(a, b string) int {
        return cmp.Compare(len(a), len(b))
    }
    slices.SortFunc(fruits, lenCmp)
    fmt.Println(fruits)
    type Person struct {
        name string
        age  int
    }
    people := []Person{
        Person{name: "Jax", age: 37},
        Person{name: "TJ", age: 25},
        Person{name: "Alex", age: 72},
    }
    slices.SortFunc(people,
        func(a, b Person) int {
            return cmp.Compare(a.age, b.age)
        })
    fmt.Println(people)
}`}
  sections={[
    {text: "We implement a comparison function for string lengths. cmp.Compare is helpful for this."},
    {text: "Now we can call slices.SortFunc with this custom comparison function to sort fruits by name length."},
    {text: "We can use the same technique to sort a slice of values that aren't built-in types."},
    {text: "Sort people by age using slices.SortFunc.  Note: if the Person struct is large, you may want the slice to contain *Person instead and adjust the sorting function accordingly. If in doubt, [benchmark](testing-and-benchmarking)!"},
    {text: "Saída:\n$ go run sorting-by-functions.go \n[kiwi peach banana]\n[{TJ 25} {Jax 37} {Alex 72}]"}
  ]}
/>

<a href="https://go.dev/play/p/3EaTknAZHMu" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Panic](/go/panic).
