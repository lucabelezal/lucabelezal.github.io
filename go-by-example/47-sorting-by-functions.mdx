---
title: Ordenação com Funções
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Ordenação com Funções"}
  code={`package main

import (
    "cmp"
    "fmt"
    "slices"
)

func main() {
    fruits := []string{"peach", "banana", "kiwi"}
    lenCmp := func(a, b string) int {
        return cmp.Compare(len(a), len(b))
    }
    slices.SortFunc(fruits, lenCmp)
    fmt.Println(fruits)
    type Person struct {
        name string
        age  int
    }
    people := []Person{
        Person{name: "Jax", age: 37},
        Person{name: "TJ", age: 25},
        Person{name: "Alex", age: 72},
    }
    slices.SortFunc(people,
        func(a, b Person) int {
            return cmp.Compare(a.age, b.age)
        })
    fmt.Println(people)
}`}
  sections={[
    {text: "Implementamos comparação por tamanho de string. `cmp.Compare` ajuda."},
    {text: "Agora chamamos `slices.SortFunc` com essa comparação para ordenar `fruits` por tamanho."},
    {text: "Podemos usar a mesma técnica para ordenar slices de tipos não embutidos."},
    {text: "Ordene `people` por idade com `slices.SortFunc`. Se `Person` for grande, prefira `*Person` e ajuste a função. Em dúvida, faça [benchmark](testing-and-benchmarking)!"},
    {text: "Saída:\n$ go run sorting-by-functions.go \n[kiwi peach banana]\n[{TJ 25} {Jax 37} {Alex 72}]"}
  ]}
/>

<a href="https://go.dev/play/p/3EaTknAZHMu" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
