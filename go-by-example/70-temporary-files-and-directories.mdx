---
title: Arquivos e Diretórios Temporários
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Arquivos e Diretórios Temporários"}
  code={`package main

import (
    "fmt"
    "os"
    "path/filepath"
)

func check(e error) {
    if e != nil {
        panic(e)
    }
}
func main() {
    f, err := os.CreateTemp("", "sample")
    check(err)
    fmt.Println("Temp file name:", f.Name())
    defer os.Remove(f.Name())
    _, err = f.Write([]byte{1, 2, 3, 4})
    check(err)
    dname, err := os.MkdirTemp("", "sampledir")
    check(err)
    fmt.Println("Temp dir name:", dname)
    defer os.RemoveAll(dname)
    fname := filepath.Join(dname, "file1")
    err = os.WriteFile(fname, []byte{1, 2}, 0666)
    check(err)
}`}
  sections={[
    {text: "A forma mais fácil de criar um arquivo temporário é com `os.CreateTemp`. Ele cria o arquivo *e* já abre para leitura/escrita. Passamos `\"\"` como primeiro arg, então `os.CreateTemp` cria no diretório padrão do OS."},
    {text: "Exiba o nome do arquivo temporário. Em Unix será `/tmp`. O nome começa com o prefixo do segundo arg e o resto é automático para garantir nomes únicos."},
    {text: "Limpe o arquivo ao terminar. O OS deve limpar temporários após um tempo, mas é boa prática fazer explicitamente."},
    {text: "Podemos escrever dados no arquivo."},
    {text: "Se for escrever muitos temporários, prefira diretório temporário. Args de `os.MkdirTemp` são iguais aos de `CreateTemp`, mas retorna *nome* de diretório."},
    {text: "Agora podemos sintetizar nomes prefixando com o diretório temporário."},
    {text: "Saída:\n$ go run temporary-files-and-directories.go\nTemp file name: /tmp/sample610887201\nTemp dir name: /tmp/sampledir898854668"}
  ]}
/>

<a href="https://go.dev/play/p/hVcPg9RH3_V" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Embed Directive](/go/embed-directive).
