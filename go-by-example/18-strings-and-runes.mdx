---
title: Strings e Runes
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Strings e Runes"}
  code={`package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
    const s = "สวัสดี"
    fmt.Println("Len:", len(s))
    for i := 0; i < len(s); i++ {
        fmt.Printf("%x ", s[i])
    }
    fmt.Println()
    fmt.Println("Rune count:", utf8.RuneCountInString(s))
    for idx, runeValue := range s {
        fmt.Printf("%#U starts at %d\n", runeValue, idx)
    }
    fmt.Println("\nUsing DecodeRuneInString")
    for i, w := 0, 0; i < len(s); i += w {
        runeValue, width := utf8.DecodeRuneInString(s[i:])
        fmt.Printf("%#U starts at %d\n", runeValue, i)
        w = width
        examineRune(runeValue)
    }
}
func examineRune(r rune) {
    if r == 't' {
        fmt.Println("found tee")
    } else if r == 'ส' {
        fmt.Println("found so sua")
    }
}`}
  sections={[
    {text: "`s` é uma `string` com o literal da palavra \"olá\" em tailandês. Literais de string em Go são UTF-8."},
    {text: "Como strings equivalem a `[]byte`, isso retorna o tamanho em bytes brutos."},
    {text: "Indexar uma string retorna os bytes brutos. Este loop gera os valores hex de todos os bytes que formam os code points em `s`."},
    {text: "Para contar _runes_ em uma string, use o pacote `utf8`. O tempo de `RuneCountInString` depende do tamanho, pois decodifica cada rune sequencialmente. Alguns caracteres tailandeses ocupam múltiplos bytes, então o resultado pode surpreender."},
    {text: "Um loop `range` trata strings de forma especial, decodificando cada `rune` com seu offset."},
    {text: "Podemos fazer a mesma iteração usando explicitamente `utf8.DecodeRuneInString`."},
    {text: "Isso demonstra passar um `rune` para uma função."},
    {text: "Valores entre aspas simples são _rune literals_. Podemos comparar um `rune` diretamente com um literal."},
    {text: "Saída:\n$ go run strings-and-runes.go\nLen: 18\ne0 b8 aa e0 b8 a7 e0 b8 b1 e0 b8 aa e0 b8 94 e0 b8 b5 \nRune count: 6\nU+0E2A 'ส' starts at 0\nU+0E27 'ว' starts at 3\nU+0E31 'ั' starts at 6\nU+0E2A 'ส' starts at 9\nU+0E14 'ด' starts at 12\nU+0E35 'ี' starts at 15\n\nUsing DecodeRuneInString\nU+0E2A 'ส' starts at 0\nfound so sua\nU+0E27 'ว' starts at 3\nU+0E31 'ั' starts at 6\nU+0E2A 'ส' starts at 9\nfound so sua\nU+0E14 'ด' starts at 12\nU+0E35 'ี' starts at 15"}
  ]}
/>

<a href="https://go.dev/play/p/-iNDXZ9IM3s" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
