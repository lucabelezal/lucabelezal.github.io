---
title: Múltiplos Retornos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Múltiplos Retornos"}
  code={`package main

import "fmt"

func vals() (int, int) {
    return 3, 7
}
func main() {
    a, b := vals()
    fmt.Println(a)
    fmt.Println(b)
    _, c := vals()
    fmt.Println(c)
}`}
  sections={[
    {text: "O `(int, int)` na assinatura mostra que a função retorna 2 `int`s."},
    {text: "Aqui usamos os 2 retornos com _multiple assignment_."},
    {text: "Se quiser apenas um subconjunto dos retornos, use o identificador `_`."},
    {text: "Saída:\n$ go run multiple-return-values.go\n3\n7\n7\n\n# Accepting a variable number of arguments is another nice\n# feature of Go functions; we'll look at this next."}
  ]}
/>

<a href="https://go.dev/play/p/vZdUvLB1WbK" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
