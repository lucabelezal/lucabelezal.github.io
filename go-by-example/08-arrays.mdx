---
title: Arrays
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Arrays"}
  code={`package main

import "fmt"

func main() {
    var a [5]int
    fmt.Println("emp:", a)
    a[4] = 100
    fmt.Println("set:", a)
    fmt.Println("get:", a[4])
    fmt.Println("len:", len(a))
    b := [5]int{1, 2, 3, 4, 5}
    fmt.Println("dcl:", b)
    b = [...]int{1, 2, 3, 4, 5}
    fmt.Println("dcl:", b)
    b = [...]int{100, 3: 400, 500}
    fmt.Println("idx:", b)
    var twoD [2][3]int
    for i := range 2 {
        for j := range 3 {
            twoD[i][j] = i + j
        }
    }
    fmt.Println("2d: ", twoD)
    twoD = [2][3]int{
        {1, 2, 3},
        {1, 2, 3},
    }
    fmt.Println("2d: ", twoD)
}`}
  sections={[
    {text: "Aqui criamos um array `a` que guarda exatamente 5 `int`s. O tipo dos elementos e o tamanho fazem parte do tipo do array. Por padrão um array tem zero value, que para `int` significa `0`s."},
    {text: "Podemos atribuir um valor em um índice com `array[index] = value` e ler com `array[index]`."},
    {text: "O builtin `len` retorna o tamanho do array."},
    {text: "Use esta sintaxe para declarar e inicializar um array em uma linha."},
    {text: "Você também pode deixar o compilador contar os elementos com `...`"},
    {text: "Se você especificar o índice com `:`, os elementos intermediários serão zerados."},
    {text: "Tipos de array são unidimensionais, mas você pode compor tipos para construir estruturas multidimensionais."},
    {text: "Você também pode criar e inicializar arrays multidimensionais de uma vez."},
    {text: "Saída:\n# Note that arrays appear in the form `[v1 v2 v3 ...]`\n# when printed with `fmt.Println`.\n$ go run arrays.go\nemp: [0 0 0 0 0]\nset: [0 0 0 0 100]\nget: 100\nlen: 5\ndcl: [1 2 3 4 5]\ndcl: [1 2 3 4 5]\nidx: [100 0 0 400 500]\n2d:  [[0 1 2] [1 2 3]]\n2d:  [[1 2 3] [1 2 3]]"}
  ]}
/>

<a href="https://go.dev/play/p/-NFSggT7dFH" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Slices](/go/slices).
