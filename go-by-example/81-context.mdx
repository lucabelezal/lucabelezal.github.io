---
title: Context
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Context"}
  code={`package main

import (
    "fmt"
    "net/http"
    "time"
)

func hello(w http.ResponseWriter, req *http.Request) {
    ctx := req.Context()
    fmt.Println("server: hello handler started")
    defer fmt.Println("server: hello handler ended")
    select {
    case <-time.After(10 * time.Second):
        fmt.Fprintf(w, "hello\n")
    case <-ctx.Done():
        err := ctx.Err()
        fmt.Println("server:", err)
        internalError := http.StatusInternalServerError
        http.Error(w, err.Error(), internalError)
    }
}
func main() {
    http.HandleFunc("/hello", hello)
    http.ListenAndServe(":8090", nil)
}`}
  sections={[
    {text: "No exemplo anterior vimos um [servidor HTTP](http-server) simples. Servidores HTTP são bons para demonstrar `context.Context` para controle de cancelamento. Um `Context` carrega deadlines, sinais de cancelamento e valores do request através de boundaries de API e goroutines."},
    {text: "Um `context.Context` é criado para cada request pelo `net/http` e fica disponível via `Context()`."},
    {text: "Aguarde alguns segundos antes de responder ao cliente. Isso simula trabalho do servidor. Enquanto trabalha, observe o channel `Done()` do context para sinal de cancelamento e retorne o quanto antes."},
    {text: "O método `Err()` do context retorna erro explicando por que `Done()` foi fechado."},
    {text: "Como antes, registramos o handler em \"/hello\" e iniciamos o serviço."},
    {text: "Saída:\n# Run the server in the background.\n$ go run context.go &\n\n# Simulate a client request to `/hello`, hitting\n# Ctrl+C shortly after starting to signal\n# cancellation.\n$ curl localhost:8090/hello\nserver: hello handler started\n^C\nserver: context canceled\nserver: hello handler ended"}
  ]}
/>

<a href="https://go.dev/play/p/7G1TlQrnbF1" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
