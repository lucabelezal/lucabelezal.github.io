---
title: Worker Pools
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Worker Pools"}
  code={`package main

import (
    "fmt"
    "time"
)

func worker(id int, jobs <-chan int, results chan<- int) {
    for j := range jobs {
        fmt.Println("worker", id, "started  job", j)
        time.Sleep(time.Second)
        fmt.Println("worker", id, "finished job", j)
        results <- j * 2
    }
}
func main() {
    const numJobs = 5
    jobs := make(chan int, numJobs)
    results := make(chan int, numJobs)
    for w := 1; w <= 3; w++ {
        go worker(w, jobs, results)
    }
    for j := 1; j <= numJobs; j++ {
        jobs <- j
    }
    close(jobs)
    for a := 1; a <= numJobs; a++ {
        <-results
    }
}`}
  sections={[
    {text: "Aqui está o worker, do qual rodaremos várias instâncias concorrentes. Eles recebem trabalho em `jobs` e enviam resultados em `results`. Dormimos 1s por job para simular tarefa custosa."},
    {text: "Para usar o pool, precisamos enviar trabalho e coletar resultados. Criamos 2 channels para isso."},
    {text: "Isso inicia 3 workers, inicialmente bloqueados sem jobs."},
    {text: "Aqui enviamos 5 `jobs` e fechamos o channel para indicar fim do trabalho."},
    {text: "Por fim coletamos todos os resultados. Isso também garante que as goroutines terminaram. Outra forma de esperar é usar [WaitGroup](waitgroups)."},
    {text: "Saída:\n# Our running program shows the 5 jobs being executed by\n# various workers. The program only takes about 2 seconds\n# despite doing about 5 seconds of total work because\n# there are 3 workers operating concurrently.\n$ time go run worker-pools.go \nworker 1 started  job 1\nworker 2 started  job 2\nworker 3 started  job 3\nworker 1 finished job 1\nworker 1 started  job 4\nworker 2 finished job 2\nworker 2 started  job 5\nworker 3 finished job 3\nworker 1 finished job 4\nworker 2 finished job 5\n\nreal\\t0m2.358s"}
  ]}
/>

<a href="https://go.dev/play/p/hiSJJsYZJKL" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
