---
title: Slices
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Slices"}
  code={`package main

import (
    "fmt"
    "slices"
)

func main() {
    var s []string
    fmt.Println("uninit:", s, s == nil, len(s) == 0)
    s = make([]string, 3)
    fmt.Println("emp:", s, "len:", len(s), "cap:", cap(s))
    s[0] = "a"
    s[1] = "b"
    s[2] = "c"
    fmt.Println("set:", s)
    fmt.Println("get:", s[2])
    fmt.Println("len:", len(s))
    s = append(s, "d")
    s = append(s, "e", "f")
    fmt.Println("apd:", s)
    c := make([]string, len(s))
    copy(c, s)
    fmt.Println("cpy:", c)
    l := s[2:5]
    fmt.Println("sl1:", l)
    l = s[:5]
    fmt.Println("sl2:", l)
    l = s[2:]
    fmt.Println("sl3:", l)
    t := []string{"g", "h", "i"}
    fmt.Println("dcl:", t)
    t2 := []string{"g", "h", "i"}
    if slices.Equal(t, t2) {
        fmt.Println("t == t2")
    }
    twoD := make([][]int, 3)
    for i := range 3 {
        innerLen := i + 1
        twoD[i] = make([]int, innerLen)
        for j := range innerLen {
            twoD[i][j] = i + j
        }
    }
    fmt.Println("2d: ", twoD)
}`}
  sections={[
    {text: "Diferente de arrays, slices são tipados apenas pelos elementos que contêm (não pelo número de elementos). Um slice não inicializado é `nil` e tem tamanho 0."},
    {text: "Para criar um slice com tamanho não-zero, use o builtin `make`. Aqui criamos um slice de `string`s de tamanho `3` (inicialmente zero-valued). Por padrão a capacidade é igual ao tamanho; se souber que o slice vai crescer, pode passar a capacidade explicitamente como parâmetro adicional para `make`."},
    {text: "Podemos atribuir e ler como em arrays."},
    {text: "`len` retorna o tamanho do slice como esperado."},
    {text: "Além dessas operações básicas, slices suportam várias outras que os tornam mais ricos que arrays. Uma delas é o builtin `append`, que retorna um slice contendo um ou mais novos valores. Note que precisamos capturar o retorno de `append`, pois podemos receber um novo slice."},
    {text: "Slices support a \"slice\" operator with the syntax `slice[low:high]`. For example, this gets a slice of the elements `s[2]`, `s[3]`, and `s[4]`."},
    {text: "Isso fatia até (excluindo) `s[5]`."},
    {text: "E isso fatia a partir de (incluindo) `s[2]`."},
    {text: "Também podemos declarar e inicializar um slice em uma única linha."},
    {text: "O pacote `slices` contém várias funções utilitárias para slices."},
    {text: "Slices podem ser compostos em estruturas multidimensionais. O tamanho dos slices internos pode variar, diferente de arrays multidimensionais."},
    {text: "Saída:\n# Note that while slices are different types than arrays,\n# they are rendered similarly by `fmt.Println`.\n$ go run slices.go\nuninit: [] true true\nemp: [  ] len: 3 cap: 3\nset: [a b c]\nget: c\nlen: 3\napd: [a b c d e f]\ncpy: [a b c d e f]\nsl1: [c d e]\nsl2: [a b c d e]\nsl3: [c d e f]\ndcl: [g h i]\nt == t2\n2d:  [[0] [1 2] [2 3 4]]\n\n# Check out this [great blog post](https://go.dev/blog/slices-intro)\n# by the Go team for more details on the design and\n# implementation of slices in Go.\n\n# Now that we've seen arrays and slices we'll look at\n# Go's other key builtin data structure: maps."}
  ]}
/>

<a href="https://go.dev/play/p/9-U3-8sKQun" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Maps](/go/maps).
