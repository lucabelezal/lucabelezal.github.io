---
title: XML
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"XML"}
  code={`package main

import (
    "encoding/xml"
    "fmt"
)

type Plant struct {
    XMLName xml.Name \`xml:"plant"\`
    Id      int      \`xml:"id,attr"\`
    Name    string   \`xml:"name"\`
    Origin  []string \`xml:"origin"\`
}

func (p Plant) String() string {
    return fmt.Sprintf("Plant id=%v, name=%v, origin=%v",
        p.Id, p.Name, p.Origin)
}
func main() {
    coffee := &Plant{Id: 27, Name: "Coffee"}
    coffee.Origin = []string{"Ethiopia", "Brazil"}
    out, _ := xml.MarshalIndent(coffee, " ", "  ")
    fmt.Println(string(out))
    fmt.Println(xml.Header + string(out))
    var p Plant
    if err := xml.Unmarshal(out, &p); err != nil {
        panic(err)
    }
    fmt.Println(p)
    tomato := &Plant{Id: 81, Name: "Tomato"}
    tomato.Origin = []string{"Mexico", "California"}
    nesting := &Nesting{}
    nesting.Plants = []*Plant{coffee, tomato}
    out, _ = xml.MarshalIndent(nesting, " ", "  ")
    fmt.Println(string(out))
}`}
  sections={[
    {text: "Plant será mapeado para XML. Como nos exemplos de JSON, tags contêm diretivas para encoder e decoder. Aqui usamos recursos especiais: `XMLName` dita o nome do elemento; `id,attr` significa que `Id` é _atributo_ XML em vez de elemento aninhado."},
    {text: "Emita XML da nossa plant; use `MarshalIndent` para saída mais legível."},
    {text: "Para adicionar header XML genérico, anexe explicitamente."},
    {text: "Use `Unmarshal` para parsear XML em estrutura. Se malformado ou incompatível, retorna erro descritivo."}
  ]}
/>

<a href="https://go.dev/play/p/vsP5mIrNJOG" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Time](/go/time).
