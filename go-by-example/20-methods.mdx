---
title: Métodos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Métodos"}
  code={`package main

import "fmt"

type rect struct {
    width, height int
}

func (r *rect) area() int {
    return r.width * r.height
}
func (r rect) perim() int {
    return 2*r.width + 2*r.height
}
func main() {
    r := rect{width: 10, height: 5}
    fmt.Println("area: ", r.area())
    fmt.Println("perim:", r.perim())
    rp := &r
    fmt.Println("area: ", rp.area())
    fmt.Println("perim:", rp.perim())
}`}
  sections={[
    {text: "O método `area` tem _receiver_ do tipo `*rect`."},
    {text: "Métodos podem ter receiver por valor ou ponteiro. Aqui um exemplo com receiver por valor."},
    {text: "Aqui chamamos os 2 métodos definidos para o struct."},
    {text: "Go converte automaticamente entre valores e ponteiros em chamadas de método. Use receiver por ponteiro para evitar cópia ou permitir mutação."},
    {text: "Saída:\n$ go run methods.go \narea:  50\nperim: 30\narea:  50\nperim: 30\n\n# Next we'll look at Go's mechanism for grouping and\n# naming related sets of methods: interfaces."}
  ]}
/>

<a href="https://go.dev/play/p/4wmDCAydC1e" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
