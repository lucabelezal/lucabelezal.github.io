---
title: Laços For
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Laços For"}
  code={`package main

import "fmt"

func main() {
    i := 1
    for i <= 3 {
        fmt.Println(i)
        i = i + 1
    }

    for j := 0; j < 3; j++ {
        fmt.Println(j)
    }

    for i := range 3 {
        fmt.Println("range", i)
    }

    for {
        fmt.Println("loop")
        break
    }

    for n := range 6 {
        if n%2 == 0 {
            continue
        }
        fmt.Println(n)
    }
}`}
  sections={[
    {text: "O tipo mais básico, com uma única condição."},
    {text: "Um laço clássico com inicialização/condição/pós-ação."},
    {text: "Outra forma de fazer a iteração básica \"faça N vezes\" é usar `range` sobre um inteiro."},
    {text: "`for` sem condição repete indefinidamente até você usar `break` ou `return` da função."},
    {text: "Você também pode usar `continue` para ir à próxima iteração."},
    {text: "Saída:\n$ go run for.go\n1\n2\n3\n0\n1\n2\nrange 0\nrange 1\nrange 2\nloop\n1\n3\n5\n\n# We'll see some other `for` forms later when we look at\n# `range` statements, channels, and other data\n# structures."}
  ]}
/>

<a href="https://go.dev/play/p/_F2rYHNilKa" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
