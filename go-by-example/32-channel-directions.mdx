---
title: Direção de Channels
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Direção de Channels"}
  code={`package main

import "fmt"

func ping(pings chan<- string, msg string) {
    pings <- msg
}
func pong(pings <-chan string, pongs chan<- string) {
    msg := <-pings
    pongs <- msg
}
func main() {
    pings := make(chan string, 1)
    pongs := make(chan string, 1)
    ping(pings, "passed message")
    pong(pings, pongs)
    fmt.Println(<-pongs)
}`}
  sections={[
    {text: "A função `ping` só aceita channel para envio. Tentar receber seria erro de compilação."},
    {text: "A função `pong` aceita um channel para receber (`pings`) e outro para enviar (`pongs`)."},
    {text: "Saída:\n$ go run channel-directions.go\npassed message"}
  ]}
/>

<a href="https://go.dev/play/p/mjNJDHwUH4R" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Select](/go/select).
