---
title: Enums
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Enums"}
  code={`package main

import "fmt"

type ServerState int

const (
    StateIdle ServerState = iota
    StateConnected
    StateError
    StateRetrying
)

var stateName = map[ServerState]string{
    StateIdle:      "idle",
    StateConnected: "connected",
    StateError:     "error",
    StateRetrying:  "retrying",
}

func (ss ServerState) String() string {
    return stateName[ss]
}
func main() {
    ns := transition(StateIdle)
    fmt.Println(ns)
    ns2 := transition(ns)
    fmt.Println(ns2)
}
func transition(s ServerState) ServerState {
    switch s {
    case StateIdle:
        return StateConnected
    case StateConnected, StateRetrying:
        return StateIdle
    case StateError:
        return StateError
    default:
        panic(fmt.Errorf("unknown state: %s", s))
    }
}`}
  sections={[
    {text: "Nosso enum `ServerState` tem tipo subjacente `int`."},
    {text: "Os valores possíveis são constantes. A keyword `iota` gera valores sucessivos automaticamente; aqui 0, 1, 2, etc."},
    {text: "Ao implementar `fmt.Stringer`, valores de `ServerState` podem ser impressos ou convertidos para string. Com muitos valores, use a ferramenta [stringer](https://pkg.go.dev/golang.org/x/tools/cmd/stringer) com `go:generate`. Veja [este post](https://eli.thegreenplace.net/2021/a-comprehensive-guide-to-go-generate)."},
    {text: "Se temos um `int`, não podemos passar para `transition` — o compilador reclama de tipo incompatível. Isso dá alguma segurança de tipo em tempo de compilação."},
    {text: "`transition` emula transição de estado de um servidor; recebe o estado atual e retorna o novo."},
    {text: "Suponha que verificamos predicados aqui para determinar o próximo estado..."},
    {text: "Saída:\n$ go run enums.go\nconnected\nidle"}
  ]}
/>

<a href="https://go.dev/play/p/prQMptP_p1s" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Struct Embedding](/go/struct-embedding).
