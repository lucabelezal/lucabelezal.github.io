---
title: Variables
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Variables"}
  code={`package main

import "fmt"

func main() {
    var a = "initial"
    fmt.Println(a)
    var b, c int = 1, 2
    fmt.Println(b, c)
    var d = true
    fmt.Println(d)
    var e int
    fmt.Println(e)
    f := "apple"
    fmt.Println(f)
}`}
  sections={[
    {text: "`var` declara una o más variables."},
    {text: "Puedes declarar varias variables a la vez."},
    {text: "Go inferirá el tipo de las variables inicializadas."},
    {text: "Las variables declaradas sin una inicialización correspondiente tienen un _valor cero_. Por ejemplo, el valor cero de un `int` es `0`."},
    {text: "La sintaxis `:=` es una forma abreviada de declarar e inicializar una variable, como `var f string = \"apple\"` en este caso. Esta sintaxis solo está disponible dentro de funciones."},
    {text: "Salida:\n$ go run variables.go\ninitial\n1 2\ntrue\n0\napple"}
  ]}
/>

<a href="https://go.dev/play/p/N5rWndIliJW" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Siguiente: [Constantes](/go/constants).
