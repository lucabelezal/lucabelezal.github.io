---
title: Slices
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Slices"}
  code={`package main

import (
    "fmt"
    "slices"
)

func main() {
    var s []string
    fmt.Println("uninit:", s, s == nil, len(s) == 0)
    s = make([]string, 3)
    fmt.Println("emp:", s, "len:", len(s), "cap:", cap(s))
    s[0] = "a"
    s[1] = "b"
    s[2] = "c"
    fmt.Println("set:", s)
    fmt.Println("get:", s[2])
    fmt.Println("len:", len(s))
    s = append(s, "d")
    s = append(s, "e", "f")
    fmt.Println("apd:", s)
    c := make([]string, len(s))
    copy(c, s)
    fmt.Println("cpy:", c)
    l := s[2:5]
    fmt.Println("sl1:", l)
    l = s[:5]
    fmt.Println("sl2:", l)
    l = s[2:]
    fmt.Println("sl3:", l)
    t := []string{"g", "h", "i"}
    fmt.Println("dcl:", t)
    t2 := []string{"g", "h", "i"}
    if slices.Equal(t, t2) {
        fmt.Println("t == t2")
    }
    twoD := make([][]int, 3)
    for i := range 3 {
        innerLen := i + 1
        twoD[i] = make([]int, innerLen)
        for j := range innerLen {
            twoD[i][j] = i + j
        }
    }
    fmt.Println("2d: ", twoD)
}`}
  sections={[
    {text: "A diferencia de los arreglos, los slices se tipan solo por los elementos que contienen, no por el número de elementos. Un slice no inicializado es igual a nil y tiene longitud 0."},
    {text: "Para crear un slice con longitud distinta de cero, usa la función incorporada `make`. Aquí creamos un slice de `string` con longitud `3`, inicialmente con valores cero. Por defecto, la capacidad de un slice nuevo es igual a su longitud; si sabemos que crecerá, podemos pasar la capacidad explícitamente como parámetro adicional de `make`."},
    {text: "Podemos asignar y obtener valores igual que con los arreglos."},
    {text: "Como era de esperar, `len` devuelve la longitud del slice."},
    {text: "Además de estas operaciones básicas, los slices admiten varias operaciones que los hacen más completos que los arreglos. Una es la función incorporada `append`, que devuelve un slice con uno o más valores nuevos. Debemos aceptar el valor devuelto por `append`, porque podría crear un slice nuevo."},
    {text: "Los slices admiten el operador de \"slice\" con la sintaxis `slice[low:high]`. Por ejemplo, esto obtiene un slice con los elementos `s[2]`, `s[3]` y `s[4]`."},
    {text: "Esto obtiene elementos hasta `s[5]`, sin incluirlo."},
    {text: "Y esto obtiene elementos desde `s[2]`, incluyéndolo."},
    {text: "También podemos declarar e inicializar una variable de tipo slice en una sola línea."},
    {text: "El paquete `slices` contiene varias funciones de utilidad para slices."},
    {text: "Los slices pueden combinarse para formar estructuras de datos multidimensionales. La longitud de los slices internos puede variar, a diferencia de los arreglos multidimensionales."},
    {text: "Saída:\n# Note that while slices are different types than arrays,\n# they are rendered similarly by `fmt.Println`.\n$ go run slices.go\nuninit: [] true true\nemp: [  ] len: 3 cap: 3\nset: [a b c]\nget: c\nlen: 3\napd: [a b c d e f]\ncpy: [a b c d e f]\nsl1: [c d e]\nsl2: [a b c d e]\nsl3: [c d e f]\ndcl: [g h i]\nt == t2\n2d:  [[0] [1 2] [2 3 4]]\n\n# Check out this [great blog post](https://go.dev/blog/slices-intro)\n# by the Go team for more details on the design and\n# implementation of slices in Go.\n\n# Now that we've seen arrays and slices we'll look at\n# Go's other key builtin data structure: maps."}
  ]}
/>

<a href="https://go.dev/play/p/9-U3-8sKQun" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
