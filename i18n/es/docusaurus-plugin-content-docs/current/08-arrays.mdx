---
title: Arreglos
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Arreglos"}
  code={`package main

import "fmt"

func main() {
    var a [5]int
    fmt.Println("emp:", a)
    a[4] = 100
    fmt.Println("set:", a)
    fmt.Println("get:", a[4])
    fmt.Println("len:", len(a))
    b := [5]int{1, 2, 3, 4, 5}
    fmt.Println("dcl:", b)
    b = [...]int{1, 2, 3, 4, 5}
    fmt.Println("dcl:", b)
    b = [...]int{100, 3: 400, 500}
    fmt.Println("idx:", b)
    var twoD [2][3]int
    for i := range 2 {
        for j := range 3 {
            twoD[i][j] = i + j
        }
    }
    fmt.Println("2d: ", twoD)
    twoD = [2][3]int{
        {1, 2, 3},
        {1, 2, 3},
    }
    fmt.Println("2d: ", twoD)
}`}
  sections={[
    {text: "Aquí creamos un arreglo `a` que contendrá exactamente 5 `int`. El tipo de sus elementos y su longitud forman parte del tipo del arreglo. Por defecto, un arreglo tiene valor cero; para los `int`, esto significa `0`."},
    {text: "Podemos asignar un valor a un índice con la sintaxis `array[index] = value` y obtenerlo con `array[index]`."},
    {text: "La función incorporada `len` devuelve la longitud de un arreglo."},
    {text: "Usa esta sintaxis para declarar e inicializar un arreglo en una sola línea."},
    {text: "También puedes hacer que el compilador cuente el número de elementos usando `...`."},
    {text: "Si especificas el índice con `:`, los elementos intermedios se inicializan con cero."},
    {text: "Los tipos de arreglo son unidimensionales, pero puedes combinar tipos para construir estructuras de datos multidimensionales."},
    {text: "También puedes crear e inicializar arreglos multidimensionales de una vez."},
    {text: "Saída:\n# Note that arrays appear in the form `[v1 v2 v3 ...]`\n# when printed with `fmt.Println`.\n$ go run arrays.go\nemp: [0 0 0 0 0]\nset: [0 0 0 0 100]\nget: 100\nlen: 5\ndcl: [1 2 3 4 5]\ndcl: [1 2 3 4 5]\nidx: [100 0 0 400 500]\n2d:  [[0 1 2] [1 2 3]]\n2d:  [[1 2 3] [1 2 3]]"}
  ]}
/>

<a href="https://go.dev/play/p/-NFSggT7dFH" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
