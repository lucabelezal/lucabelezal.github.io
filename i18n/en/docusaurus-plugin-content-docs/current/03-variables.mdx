---
title: Variables
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Variables"}
  code={`package main

import "fmt"

func main() {
    var a = "initial"
    fmt.Println(a)
    var b, c int = 1, 2
    fmt.Println(b, c)
    var d = true
    fmt.Println(d)
    var e int
    fmt.Println(e)
    f := "apple"
    fmt.Println(f)
}`}
  sections={[
    {text: "`var` declares 1 or more variables."},
    {text: "You can declare multiple variables at once."},
    {text: "Go will infer the type of initialized variables."},
    {text: "Variables declared without a corresponding initialization are _zero-valued_. For example, the zero value for an `int` is `0`."},
    {text: "The `:=` syntax is shorthand for declaring and initializing a variable, e.g. for `var f string = \"apple\"` in this case. This syntax is only available inside functions."},
    {text: "Saída:\n$ go run variables.go\ninitial\n1 2\ntrue\n0\napple"}
  ]}
/>

<a href="https://go.dev/play/p/N5rWndIliJW" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
