---
title: Methods
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Methods"}
  code={`package main

import "fmt"

type rect struct {
    width, height int
}

func (r *rect) area() int {
    return r.width * r.height
}
func (r rect) perim() int {
    return 2*r.width + 2*r.height
}
func main() {
    r := rect{width: 10, height: 5}
    fmt.Println("area: ", r.area())
    fmt.Println("perim:", r.perim())
    rp := &r
    fmt.Println("area: ", rp.area())
    fmt.Println("perim:", rp.perim())
}`}
  sections={[
    {text: "This `area` method has a _receiver type_ of `*rect`."},
    {text: "Methods can be defined for either pointer or value receiver types. Here's an example of a value receiver."},
    {text: "Here we call the 2 methods defined for our struct."},
    {text: "Go automatically handles conversion between values and pointers for method calls. You may want to use a pointer receiver type to avoid copying on method calls or to allow the method to mutate the receiving struct."},
    {text: "Saída:\n$ go run methods.go \narea:  50\nperim: 30\narea:  50\nperim: 30\n\n# Next we'll look at Go's mechanism for grouping and\n# naming related sets of methods: interfaces."}
  ]}
/>

<a href="https://go.dev/play/p/4wmDCAydC1e" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
