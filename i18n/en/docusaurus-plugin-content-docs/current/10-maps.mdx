---
title: Maps
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Maps"}
  code={`package main

import (
    "fmt"
    "maps"
)

func main() {
    m := make(map[string]int)
    m["k1"] = 7
    m["k2"] = 13
    fmt.Println("map:", m)
    v1 := m["k1"]
    fmt.Println("v1:", v1)
    v3 := m["k3"]
    fmt.Println("v3:", v3)
    fmt.Println("len:", len(m))
    delete(m, "k2")
    fmt.Println("map:", m)
    clear(m)
    fmt.Println("map:", m)
    _, prs := m["k2"]
    fmt.Println("prs:", prs)
    n := map[string]int{"foo": 1, "bar": 2}
    fmt.Println("map:", n)
    n2 := map[string]int{"foo": 1, "bar": 2}
    if maps.Equal(n, n2) {
        fmt.Println("n == n2")
    }
}`}
  sections={[
    {text: "Set key/value pairs using typical `name[key] = val` syntax."},
    {text: "Printing a map with e.g. `fmt.Println` will show all of its key/value pairs."},
    {text: "Get a value for a key with `name[key]`."},
    {text: "If the key doesn't exist, the [zero value](https://go.dev/ref/spec#The_zero_value) of the value type is returned."},
    {text: "The builtin `len` returns the number of key/value pairs when called on a map."},
    {text: "The builtin `delete` removes key/value pairs from a map."},
    {text: "To remove *all* key/value pairs from a map, use the `clear` builtin."},
    {text: "The optional second return value when getting a value from a map indicates if the key was present in the map. This can be used to disambiguate between missing keys and keys with zero values like `0` or `\"\"`. Here we didn't need the value itself, so we ignored it with the _blank identifier_ `_`."},
    {text: "You can also declare and initialize a new map in the same line with this syntax."},
    {text: "The `maps` package contains a number of useful utility functions for maps."},
    {text: "Saída:\n# Note that maps appear in the form `map[k:v k:v]` when\n# printed with `fmt.Println`.\n$ go run maps.go \nmap: map[k1:7 k2:13]\nv1: 7\nv3: 0\nlen: 2\nmap: map[k1:7]\nmap: map[]\nprs: false\nmap: map[bar:2 foo:1]\nn == n2"}
  ]}
/>

<a href="https://go.dev/play/p/5jpkxJ2T0Lv" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>
