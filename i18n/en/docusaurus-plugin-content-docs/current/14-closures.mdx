---
title: Closures
---

import CodeExplanation from '@site/src/components/CodeExplanation';

<CodeExplanation
  title={"Closures"}
  code={`package main

import "fmt"

func intSeq() func() int {
    i := 0
    return func() int {
        i++
        return i
    }
}
func main() {
    nextInt := intSeq()
    fmt.Println(nextInt())
    fmt.Println(nextInt())
    fmt.Println(nextInt())
    newInts := intSeq()
    fmt.Println(newInts())
}`}
  sections={[
    {text: "This function `intSeq` returns another function, which we define anonymously in the body of `intSeq`. The returned function _closes over_ the variable `i` to form a closure."},
    {text: "We call `intSeq`, assigning the result (a function) to `nextInt`. This function value captures its own `i` value, which will be updated each time we call `nextInt`."},
    {text: "See the effect of the closure by calling `nextInt` a few times."},
    {text: "To confirm that the state is unique to that particular function, create and test a new one."},
    {text: "Saída:\n$ go run closures.go\n1\n2\n3\n1\n\n# The last feature of functions we'll look at for now is\n# recursion."}
  ]}
/>

<a href="https://go.dev/play/p/NpgpzS8ZG8y" target="_blank" rel="noreferrer" style={{fontSize: 14, opacity: 0.7}}>
  Run on go.dev/play
</a>

Próximo: [Recursion](/go/recursion).
